Тесты я запустить не смог — в этой среде выполнение `python`/`pytest` не разрешено, поэтому проверка статическая: я читал код, тесты и спеки, а не наблюдал прогон.

## Что проверено по acceptance

**Критерий 1 (сценарии → тесты с докстрингами).** Все восемь сценариев `leak-scan` и два `reviewer-adapter` из `openspec/changes/archive/2026-09-17-adopter-small-defects/specs/` имеют тест, чей докстринг называет сценарий дословно. Отступление от контрактной формулировки «sole occurrence» записано в `design.md` отдельным решением — что criterion 1 и допускает.

**Критерий 2 (ничего не печатать).** `LeakHit` несёт только путь и идентификацию; путь пропускается через `safe_path` в обеих точках конструирования (`_signature_hits` и marker-ветка), так что и маркер в пути, и имя файла, совпавшее с сигнатурой, заменяются описанием. `assert_no_leaks` остался category-only, и тест пинит хвост `(aws-access-key-id)`.

**Критерий 3 (гейт и standalone не разъезжаются).** Один `render_leak_hits` на оба вызова; `tests/test_gate.py:1761` пинит обе строки в одном тесте с общим `detail`.

**Критерий 4 (самосовпадение).** Подавление по местоположению реализовано, встречный сценарий покрыт.

**Критерий 5 (диагностика ревьюера + непотревоженные пины).** `_with_diagnostics` навешана на все пути отказа после `_run_reviewer` (парсинг, несовпадение коммита, обе ветки `ReviewSubmitError`), путь именуется, пустой stderr молчит, `OSError` при сохранении не роняет уже записанный вердикт. Байт-в-байт транскрипт (`tests/test_gate.py:409`) прогоняется на чистом кандидате, где WARN-строки нет вовсе, так что изменение её формата до него не дотягивается; ожидания этого теста и пинов промпта в диффе не тронуты. Циклического импорта `capture → project` нет: `project.py` тянет только `__version__`.

Блокирующих дефектов не нашёл. Ниже — четыре необязательных замечания.

**ADV-SELF-MATCH-COUNTS-PATHS** — в `src/agentmarshal/journal/capture.py:452` подавление решается по множеству *путей* (`elsewhere = {path for path in occurrences if path != config_path}`), а не по числу вхождений: два вхождения маркера внутри самого `.agentmarshal/project.json` (одно в списке маркеров, второе в любом другом поле того же файла) неотличимы от одного, и хит не сообщается нигде. Контрактная модель угроз ограничивала сужение словами «and only when it is the sole occurrence», а записанное в `design.md` отступление обосновывает только то, что путь декларации не печатается рядом с настоящим хитом — про проглатывание второго вхождения в том же файле оно не говорит. Практический риск невелик (значение маркера и так лежит в этом файле осознанно), но обоснование не покрывает то, что код на самом деле ослабляет.

**ADV-UNBOUNDED-WARN-LINE** — в `src/agentmarshal/journal/gate.py:1152` и `src/agentmarshal/cli.py:1297` записи больше не дедуплицируются по категории: раньше маркер, встречающийся в 200 файлах кандидата, давал один токен `private-marker`, теперь — 200 записей в одной строке WARN merge-транскрипта. Ни свёртки, ни ограничения длины нет, а транскрипт в этом проекте — читаемый артефакт.

**ADV-DIFF-PREFIX-CONFIG** — `_diff_path` в `src/agentmarshal/journal/capture.py:356` снимает префикс `b/`, но вызывающие стороны не нейтрализуют конфиг репозитория: `git diff` в `gate.py:1129` и `cli.py:1275` пинит `--text --no-textconv --no-ext-diff`, но не `--no-prefix`/`-c diff.mnemonicPrefix=false`. При `diff.mnemonicPrefix = true` заголовки приходят как `+++ c/…`, путь сообщается с префиксом, и ключ подавления `.agentmarshal/project.json` перестаёт совпадать молча. То же с `core.quotePath` (включён по умолчанию) для не-ASCII путей: `+++ "b/\303\251.txt"` печатается как есть, вместе с кавычками и восьмеричными escape'ами. Направление отказа безопасное (лишний шум, не пропуск), но комментарий «callers here build the diff themselves» этот случай не закрывает — вызывающие стороны как раз этот конфиг и не глушат.

**ADV-STALE-SIDECAR-DOC** — `docs/sidecar.md:247` по-прежнему описывает строку гейта как «`WARN: possible leak in candidate additions (advisory, not blocking)` with the markers it matched»; теперь она несёт `файл: что совпало`. Файл не входит в scope контракта, так что здесь его поправить было нельзя — фиксирую как образовавшийся дрейф документации.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "945cbf4757d8676b7baafc10095932f83713de21", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-SELF-MATCH-COUNTS-PATHS", "ADV-UNBOUNDED-WARN-LINE", "ADV-DIFF-PREFIX-CONFIG", "ADV-STALE-SIDECAR-DOC"]}
AGENTMARSHAL_VERDICT_END
