I reviewed the diff against the working tree at `b87481b0`. Test execution was blocked by the sandbox, so verification is static: I read every touched source and test file, traced both `scan_diff_for_leaks` call sites, checked the import graph for a cycle (`capture.py` → `project.py` → `agentmarshal/__init__` — no cycle), and hand-checked each new assertion against the code it pins.

**What holds up.** Every scenario in `openspec/specs/leak-scan/spec.md` and the new reviewer-adapter requirement has a test whose docstring names it. `render_leak_hits` is the single renderer for both the gate line and the standalone headline, and `tests/test_gate.py:1760` pins both strings in one test, so they cannot drift. `safe_path` masks only the offending span, and `tests/test_capture.py:400` proves two files under one marker-named directory stay two hits. The self-match rule keys on `PROJECT_CONFIG_RELPATH`, one constant now read by the gate, the CLI and the scan default; the sidecar case passes `""`, which suppresses nothing — the safe direction. `_keep_diagnostics` returns `None` on empty stderr, so a silent command stays silent, and the OSError path returns a note instead of raising, so a recorded verdict survives. design.md records the departure from the contract's "sole occurrence" bound and argues it, which acceptance criterion 1 permits. All ten touched paths are inside the amended scope.

Four advisory notes, none of them blocking:

**ADV-DIAGNOSTICS-TEMP-FILE-ACCUMULATION** — `_keep_diagnostics` in `src/agentmarshal/journal/review.py:327` writes a new never-deleted `mkstemp` file on *every* zero-exit run whose reviewer wrote anything to stderr. `_preserve_output` shares the no-cleanup policy, but that one fires only on a rejection; this one fires on the success path, and vendor CLIs routinely emit progress or telemetry on stderr. A CI job that records many reviews leaves one temp file per review. design.md does not address the lifetime.

**ADV-DIFF-PREFIX-PINS-INCOMPLETE** — the comment at `src/agentmarshal/journal/gate.py:1128` says the three `-c` pins "keep the destination prefix the parser strips (`b/`)", but `diff.noprefix=false` and `diff.mnemonicPrefix=false` do not override `diff.srcPrefix`/`diff.dstPrefix` (git ≥ 2.41). A repository setting `diff.dstPrefix` still produces `+++ dst/…`, `_diff_path` returns the path unstripped, and the suppression key silently stops matching `.agentmarshal/project.json` — the exact failure the comment claims the pins prevent. The consequence is over-reporting plus a prefixed path in the warning, not a missed leak.

**ADV-QUOTEPATH-SKIPS-SCAN-ON-NON-UTF8-PATH** — adding `-c core.quotePath=false` at `gate.py:1136` and `cli.py:1283` makes git emit path bytes raw. Both `_run_git` (`gate.py:285`) and `_leak_scan_git` (`cli.py:1199`) decode the diff with strict UTF-8, so a candidate touching a file whose name is not valid UTF-8 now turns the whole advisory scan into `WARN: leak-scan skipped` (gate) or `leak-scan: git diff failed` (CLI), where the previous default escaped the path and the scan ran. Narrow and announced, but it is a security control narrowing introduced by this change and unmentioned in design.md.

**ADV-SIDECAR-DOC-DESCRIBES-OLD-WARN-SHAPE** — `docs/sidecar.md:247` still says the gate's hit line carries "the markers it matched"; it now carries `file: what matched`. `docs/` is outside this task's scope, so fixing it needs a scope amendment, but the sentence is stale as of this commit.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "b87481b02b9bd037eae16b5804a2806f93f6d4dd", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-DIAGNOSTICS-TEMP-FILE-ACCUMULATION", "ADV-DIFF-PREFIX-PINS-INCOMPLETE", "ADV-QUOTEPATH-SKIPS-SCAN-ON-NON-UTF8-PATH", "ADV-SIDECAR-DOC-DESCRIBES-OLD-WARN-SHAPE"]}
AGENTMARSHAL_VERDICT_END
