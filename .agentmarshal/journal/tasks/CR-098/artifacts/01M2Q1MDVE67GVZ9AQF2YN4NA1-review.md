Проверил статически — запустить `pytest` в этой песочнице не дали (команда требует approval), так что выводы ниже основаны на чтении кода, а не на прогоне тестов.

**Что сходится с контрактом.** Все четыре сценария из `openspec/specs/trust-preconditions/spec.md` покрыты тестами, чьи docstring их называют (`tests/test_cli.py:16`, `tests/test_doctor.py:40`, `:62`, `:103`). `doctor` действительно никогда не печатает значение reviewer-команды: `_check_reviewer_command` (`src/agentmarshal/doctor.py:147`) возвращает фиксированную строку и глотает `ReviewLaunchError` целиком, а generic-handler в `run_doctor` недостижим для этой ветки — все пути `_reviewer_command` поднимают только `ReviewLaunchError`. Exit-статус сохранён: обе проверки-предусловия, которые могут упасть по «жёсткой» причине (нет git-репозитория, нет `project.json`), дублируются non-precondition проверками, так что маскирования реальной поломки нет. `tasks.md` отмечает ровно то, что приземлилось, шаблон и gate корректно не тронуты — это совпадает с amendment'ом и с решением 5 в `design.md`. Утверждение, добавленное в `docs/self-hosting-workflow.md:118`, проверяется по `templates/github/agentmarshal-governance.yml` — `grep -oE 'CR-[0-9]+'` по `HEAD_REF` там действительно есть.

Ниже — только advisory, блокирующего не нашёл.

`ADV-STALE-NEUTRAL-PATH-RISK` — в `openspec/changes/archive/2026-09-17-report-what-the-tool-cannot-verify/design.md:39` остался риск «The neutral path hides a genuine refusal» с митигацией про отсутствие review-записи для head SHA. Это риск gate-нейтральности, которую amendment вынес в отдельную задачу; в этом change никакого neutral path нет. Архивный design теперь описывает механизм, которого он не содержит.

`ADV-PROPOSAL-IMPACT-OVERSTATED` — `proposal.md:33` перечисляет в Impact `docs/harness-setup.md`, который диффом не тронут, а секция «Why» по-прежнему ведёт с предложения 017 (фикс шаблона) без пометки, что эта половина ушла. Archived proposal описывает change шире, чем тот, что заархивирован.

`ADV-SUMMARY-CONFLATES-FAULT-AND-PRECONDITION` — `src/agentmarshal/cli.py:389` печатает `Summary: N check(s) reported unmet` для любого падения, включая настоящие поломки (нет git, нечитаемый `project.json`). Раньше было «failed». Одно число теперь смешивает «проект сломан» и «не выставлена переменная окружения», и отличить их можно только по exit-коду — а это ровно та тренировка «красное — это норма», от которой предостерегает threat model контракта. Критерий приёмки формально выполнен (строка не утверждает, что все проверки прошли), поэтому только advisory.

`ADV-ACTOR-FAIL-FOR-SOLO-HUMAN` — `_check_actor_variable` (`src/agentmarshal/doctor.py:139`) отдаёт `FAIL` всякий раз, когда источник — `git-identity`. Но `docs/quickstart.md:160` документирует «With no table the git identity is recorded as-is» как легитимную конфигурацию. Проект одного человека без агентов будет вечно видеть `FAIL: recorded actor` и `1 check(s) reported unmet`; выход есть (вписать свою identity в `actors`, тогда источник станет `project-actor` и проверка позеленеет), но ни в detail-строке это не подано как «для человека достаточно таблицы», ни в quickstart не описано. Поведение требует сам сценарий спеки, так что это не дефект реализации — но зазор между докой и отчётом стоит закрыть.

`ADV-DUPLICATE-TEST-GIT-HELPER` — `init_git_repo` теперь определён дважды, в `tests/test_cli.py:11` и `tests/test_doctor.py:19`, с разными телами: версия в `test_doctor.py` специально прибивает `user.name`/`user.email`, потому что иначе actor-проверка читает конфиг машины, а копия в `test_cli.py` этого не делает. `test_doctor_preconditions_are_report_only` сейчас проходит при обоих состояниях ambient-конфига (и с identity, и без неё проверка падает), так что флака нет — но тест зависит от окружения ровно там, где соседний файл от этой зависимости избавлялся.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "3c2116bf861636f42ea949c73462d522face0aa5", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-STALE-NEUTRAL-PATH-RISK", "ADV-PROPOSAL-IMPACT-OVERSTATED", "ADV-SUMMARY-CONFLATES-FAULT-AND-PRECONDITION", "ADV-ACTOR-FAIL-FOR-SOLO-HUMAN", "ADV-DUPLICATE-TEST-GIT-HELPER"]}
AGENTMARSHAL_VERDICT_END
