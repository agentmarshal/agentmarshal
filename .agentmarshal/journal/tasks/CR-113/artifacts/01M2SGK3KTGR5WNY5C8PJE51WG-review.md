Ревью закончено: diff можно принимать, блокирующих замечаний нет. Есть одно мелкое замечание по оформлению. Полный CI я не запускал: в этой сессии он требует одобрения.

**Критерии приёмки:**

- **Дайджест 024.**
  - Написан на английском. Псевдоним Adopter D и его профиль дословно совпадают с 014–023.
  - Источник указан через sha256, версия та же — 0.3.0.
  - По каждому из трёх предложений есть решение с обоснованием: словарь принят, утверждение принято, поле для времени сброса отложено.
  - Цифры согласуются с контрактом и индексом: 13 сессий, 12 задач, 3 модели, 2 остановки. Сверить их с исходником дословно я не мог — его нет в snapshot.
- **Индекс предложений.**
  - Добавлена таблица «Batch of 2026-09-18» с решением и колонкой «Where».
  - Как сопоставить файл из outbox с дайджестом, уже объясняет раздел «Tracking what happened to yours»: захешировать файл и найти хеш в каталоге. Раздел был в индексе и до этого diff.
- **Quickstart.**
  - Прямо сказано, что token counts — не то, что списывает провайдер.
  - Документировано значение `provider-limit`.
  - Утверждение «пустое значение отклоняется, но слово не проверяется и не подсчитывается» соответствует коду: `_validate_session_record` в `records.py` требует только непустую строку, а `report` не использует `outcome`.
  - Ссылок на старый якорь заголовка «Record what the task cost» нигде нет, так что переименование ничего не ломает.
- **README.md.** Формулировки «what each task actually cost» и «token costs» заменены.
- **Упоминания релизов и документов.**
  - 0.4.0 с тремя новыми схемами описана в CHANGELOG с датой 2026-09-18.
  - Accounting rework прямо назван незапланированным, предложение 018 существует.
  - Новый пункт в `docs/README.md` добавлен.
- **CI.**
  - Запустить `validate`, `pytest`, `ruff` и `mypy` не удалось: команды требуют одобрения.
  - Diff меняет только документацию, fenced-блоки quickstart не тронуты. Поэтому `test_quickstart.py` и проверки по тексту в `test_review_launcher.py` не должны задеваться, а на ruff и mypy изменения не влияют.

**Замечание (не блокирует):** в `docs/quickstart.md`, в абзацах сразу после «journal-only additive candidate», после замены слов строки не перенесены. Две из них получились длиннее ~80 символов, на которые рассчитан текст файла: «measurement would live in a working tree instead of in git, which is the thing this project» и «Nothing calls this for you. Recording the measurement is a step of your loop — put it».

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "ebbac7f8bd21f540eab4c761ed48370dc1d85c06", "verdict": "approved", "findings": [], "advisory_findings": ["quickstart-step7-lines-not-rewrapped"]}
AGENTMARSHAL_VERDICT_END
