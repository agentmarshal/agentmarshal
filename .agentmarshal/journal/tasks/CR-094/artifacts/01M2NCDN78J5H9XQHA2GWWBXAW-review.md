Проверил диff против контракта: десять дайджестов (014–023), все с `Observed on: 0.3.0`, все с уникальным 64-символьным lowercase sha256 в source-строке, измерения вынесены отдельным блоком, у каждого есть disposition с обоснованием. Скан по новым файлам не нашёл ни имён хостингов/репозиториев, ни путей, ни task id, ни цитат чужого кода — единственные backtick-фрагменты это собственные команды проекта (`init`, `doctor`, `leak-scan`, `record-session`, `validate`, `agentmarshal review`). Профиль Adopter D в таблице описывает setup, а не продукт или домен. Индекс новой партии содержит колонку **Where**, а 016 прямо говорит, что фикс уже на default branch и выйдет в 0.4.0. CONTRIBUTING описывает все три способа отслеживания. Сверил также фактические утверждения дайджестов с кодом: stderr ревьюера действительно отбрасывается при нулевом exit (`src/agentmarshal/journal/review.py:200-211`), leak-scan печатает только категории (`src/agentmarshal/cli.py:1147-1151`), `doctor` имеет ровно четыре проверки, шаблон gate — `continue-on-error` с извлечением task из head ref, а счёт «21 amendment records across 18 of its 91 completed tasks» в 022 сходится с журналом (22 записи в 19 задачах минус незавершённая CR-095).

Одно замечание без блокировки: сводка новой партии в `docs/proposals/README.md` говорит «all accepted — two of them in part» и что скобочную оговорку несут «018 and 020», но 016 в своём заголовке и в строке индекса тоже помечен «accepted *(in part; the third request is met in another shape)*» — то есть частично принятых по самоописанию три, а не два, и перечисление в разделе Disposition неполно.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "404ddd9175bb0cbcf1b7f349804e9049e3d1c368", "verdict": "approved", "findings": [], "advisory_findings": ["index-in-part-count-inconsistent-with-016"]}
AGENTMARSHAL_VERDICT_END
