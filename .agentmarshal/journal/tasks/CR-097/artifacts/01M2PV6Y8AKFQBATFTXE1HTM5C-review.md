Проверил дифф и код в снапшоте (тесты запустить не дал sandbox — permission prompt на `pytest`, поэтому выводы статические, но проверяемые по коду). Коротко: контракт закрыт почти весь, но dry-run сообщает о любой ошибке запуска как о непарсящемся verdict'е, а пустой плейсхолдер `{}` отказ не называет — это и есть два блокирующих пункта.

F-001 — `src/agentmarshal/cli.py:578-584`: the dry run's only failure message is `dry run could not parse reviewer verdict: {error}`, but `dry_run_review` raises `ReviewLaunchError` for every earlier step too — an unset `AGENTMARSHAL_REVIEWER_CMD`, an unsupported placeholder (`review.py:208-211`, the refusal this very change added), `cannot run reviewer`, and a non-zero exit (`review.py:249-259`). The most likely real case is a template using `{model}`: without `--model` the dry run substitutes the literal `dry-run` (`cli.py:580`), the vendor rejects the model id, and the operator is told their reviewer "could not parse a verdict" when it never produced one. The command whose purpose is diagnosing a misconfigured reviewer misnames the misconfiguration.

F-002 — `src/agentmarshal/journal/review.py:160-173` with `:205-212`: for an auto-numbered field the scan returns an empty token. `string.Formatter().parse("{}")` yields field_name `''`, `'' not in known` is true, so the refusal reads `AGENTMARSHAL_REVIEWER_CMD has an unsupported placeholder: ` and names nothing — against the spec's "the refusal SHALL name the offending token" and design.md's "reports the name that was not in the set". `tests/test_review_launcher.py:315` parametrises `unsupported` and `0`, i.e. the numbered case `{0}`, but not the bare `{}`.

F-003 — `src/agentmarshal/journal/review.py:380-392`: the dry run stops at `_parse_verdict`, while the recorded path additionally requires `reviewed_commit == resolved_commit` (`:485-488`) and passes the verdict through record validation (`:506-517`: unknown verdict value, empty findings for a non-approving verdict, duplicate or overlapping ids). A command echoing a fixed SHA, or `blocked` with `findings: []`, is reported as "has a parseable verdict" and would still lose the first real review.

F-004 — `src/agentmarshal/journal/review.py:389-391`: the dry run runs the command in an empty temporary directory, not a snapshot, so the relative-path resolution documented at `docs/quickstart.md:48-53` — the exact trap proposal 015 reports — is the one property the dry run cannot exercise: a relative command that works in a real review fails here. Neither design.md nor the docs say so.

F-005 — `docs/quickstart.md:46-59`: the new section documents the contract but never mentions `review --dry-run`, which appears nowhere outside `--help`; the objective's "exercise the command before the first real review" is unreachable from the documentation. Relatedly, `docs/quickstart.md:83-85` states that a refused verdict keeps the reviewer's raw output, which the dry run deliberately does not do (`review.py:392`).

F-006 — `openspec/changes/archive/2026-09-17-document-the-reviewer-contract/proposal.md:5`: the link `../../../docs/proposals/015-reviewer-command-contract-undocumented.md` resolves to `openspec/docs/proposals/...`, which does not exist; from the archive depth it needs four levels, as the sibling `archive/2026-09-17-render-amendment-history/proposal.md:3` uses.

F-007 — `openspec/changes/archive/2026-09-17-document-the-reviewer-contract/specs/reviewer-adapter/spec.md` and `openspec/specs/reviewer-adapter/spec.md`: acceptance criterion 1 exempts "the two documentation scenarios", but the requirement carries one scenario; the adapter-responsibility half, split out as task 1.2, has no scenario of its own.

F-008 — `src/agentmarshal/cli.py:153-166` with `:595-607`: six flags lost their argparse `required=True` and are now re-checked by hand, so a missing flag on a recorded review changes from argparse's exit 2 and standard message to exit 1 and `review: required unless --dry-run: …`. design.md records no decision about moving that enforcement, proposal.md's impact line says only "the flag", and no test covers the new message.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "fcbd305bf907ee151b2a6408729c0bc589ff0dd0", "verdict": "changes_required", "findings": ["F-001", "F-002"], "advisory_findings": ["F-003", "F-004", "F-005", "F-006", "F-007", "F-008"]}
AGENTMARSHAL_VERDICT_END
