I reviewed the diff against the contract, the two named decisions, the archived OpenSpec change and the published `reviewer-adapter` spec. Findings below.

**F-001** — `src/agentmarshal/cli.py:582` hands `dry_run_review` `Path.cwd()`, while the recorded path at `cli.py:608-613` hands `launch_review` `placement.host_root`. In a **sidecar** placement those are different repositories: `placement.py:85-98` resolves `host` from `project.json` and `docs/sidecar.md:139` instructs the operator to run *every* command from the sidecar, while `docs/sidecar.md:208-209` states that `agentmarshal review` "snapshots the host tree". So a sidecar operator's dry run extracts `HEAD` of the journal repository — or, if the sidecar is not itself a git worktree, fails at `_extract_snapshot` (`review.py:347-368`) with `git archive failed: not a git repository`, never launching the command at all. That breaks the spec requirement "`agentmarshal review --dry-run` SHALL launch the configured command on a synthetic prompt" (`openspec/specs/reviewer-adapter/spec.md:37-39`) for a supported placement, and it defeats the single reason `design.md` gives for the recorded departure — "a recorded review runs the command inside a populated snapshot, so a relative path in the template resolves there" — because the tree a relative path resolves against is the wrong one. `dry_run_review`'s own docstring (`review.py:384-385`) asserts "The command runs where a recorded review runs it", which is true only for embedded projects. No test covers a sidecar dry run.

**A-001** (advisory) — `src/agentmarshal/journal/review.py:216-220` claims the `except (KeyError, IndexError, ValueError)` branch is "Unreachable by construction: the scan above names every field the formatter would reject". It is reachable by two ordinary routes. A format-spec error such as `{model:d}` passes `_unsupported_placeholder` (field name `model` is known, the spec `d` contains no fields) and then raises `ValueError` from `.format` at line 215. More importantly, a *malformed* template — `codex exec --model {model -`, i.e. the closing brace lost to shell quoting, which is the exact incident `docs/proposals/015-...md:20-22` reports — makes `string.Formatter().parse` itself raise `ValueError` at `review.py:163`, inside the same `try`, so the operator still gets the bare "AGENTMARSHAL_REVIEWER_CMD has an invalid placeholder" with nothing to search for, and not even the offending argument. The named-token work covers unknown field names only; the reported failure mode is unchanged, and the comment tells the next maintainer it is handled.

**A-002** (advisory) — `src/agentmarshal/journal/review.py:407` parses with `preserve_output=False`, so a dry run that fails discards the reviewer's actual output entirely; the operator debugging their command learns only "reviewer output has invalid verdict sentinels" and never sees what the command printed. Nothing in the contract requires this: acceptance criterion 2 and `spec.md:37-39` bar a record, an artifact and files *in the journal*, whereas `_preserve_output` (`review.py:263-277`) writes to the system temp directory, outside any journal — and `docs/adr/ADR-0005-evidence-capture-and-format.md` records that preservation as the 0.3.0 behaviour "when a verdict is refused". This is a second departure from `design.md`'s "hands the output to the same verdict parser the real path uses", and unlike the `HEAD` snapshot it is not recorded there, which acceptance criterion 1 requires.

**A-003** (advisory) — `docs/quickstart.md:48-49` says "metadata-free snapshot" and never says what that means operationally: that the snapshot contains no `.git` at all (`review.py:347-355`). Proposal 015's first and most expensive property is precisely that "a wrapper that begins by asking git for the repository root to find its own scripts dies there, and at least one vendor CLI refuses to start in such a directory without an explicit flag" (`docs/proposals/015-...md:10-15`). The four things acceptance criterion 5 enumerates are all present, but an operator reading this paragraph still cannot predict that failure — the term of art is doing the work the sentence should.

**A-004** (advisory) — the contract's `documents` list names `openspec/changes/document-the-reviewer-contract/`, and acceptance criterion 1 quantifies over `openspec/changes/document-the-reviewer-contract/specs/reviewer-adapter/spec.md`. Neither exists at the reviewed commit: the change was created directly under `openspec/changes/archive/2026-09-17-document-the-reviewer-contract/`. The archive-and-sync is the documented workflow (`.agents/skills/openspec-archive-change/SKILL.md:130-145`) and `openspec/specs/reviewer-adapter/spec.md` carries the content, so nothing is lost — but the contract handed to this review names a path the reviewed tree does not contain, and the reviewer prompt carried it as named material.

**A-005** (advisory) — acceptance criterion 1 excepts "the two documentation scenarios", but the delivered spec has one (`openspec/specs/reviewer-adapter/spec.md:20-24`), folding both halves of the documentation requirement — the four stated facts and the adapter's responsibility — into a single scenario. The four non-documentation scenarios each have a test whose docstring names them (`tests/test_review_launcher.py:319,334,363,385`), so the criterion is satisfied in substance; the count is not.

**A-006** (advisory) — `src/agentmarshal/cli.py:150-166` drops `required=True` from `--task`, `--base`, `--role`, `--vendor`, `--model`, `--email` and from the `--commit`/`--reviewed-finding` group, replacing argparse's enforcement with the manual check at `cli.py:595-607`. A recorded `review` missing a flag now exits **1** with a flat list instead of **2** with usage, so a caller that distinguishes usage errors from runtime failures changes behaviour. `--dry-run` also silently ignores any of those flags if supplied.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "5ffb96e1ad9b0ce5efb2750d5c46121f6e981e35", "verdict": "changes_required", "findings": ["F-001"], "advisory_findings": ["A-001", "A-002", "A-003", "A-004", "A-005", "A-006"]}
AGENTMARSHAL_VERDICT_END

Кратко по-русски: основная работа сделана аккуратно — промпт для dry run строится из того же `_REVIEW_PROMPT`, пиннутый тест 0.3.0 не тронут, журнал при dry run действительно не меняется, все четыре сценария спеки покрыты тестами с называющими их docstring'ами, отступление про снапшот `HEAD` записано в `design.md`. Блокирует одно: `cli.py:582` передаёт в `dry_run_review` `Path.cwd()` вместо `placement.host_root`, поэтому в sidecar-размещении dry run снапшотит не тот репозиторий (а если sidecar не git-worktree — вообще падает на `git archive`, не запустив команду), то есть проверяет ровно не тот контракт, ради которого он и сделан. Из советов главный — A-001: случай с потерянной закрывающей скобкой из proposal 015 по-прежнему не называет ничего, а комментарий в коде утверждает обратное.

Тесты я запустить не смог — sandbox отказал в выполнении `pytest`; выводы выше получены статическим чтением.
