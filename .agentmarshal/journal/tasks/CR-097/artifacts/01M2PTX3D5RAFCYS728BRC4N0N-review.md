I reviewed the diff by reading the code in the snapshot; I could not execute the test suite (the sandbox refused the `pytest` invocation), so the acceptance criteria that turn on tests passing were checked by reading — `_REVIEW_PROMPT` and `_review_prompt` are untouched, the pinned 0.3.0 test is not in the diff, and `_dry_run_prompt` delegates to `_review_prompt`, so criterion 3 holds structurally.

**What is right:** the quickstart section states all four required things (working directory, relative-path resolution, `{prompt_file}` contents, and that bounding reads belongs to the adapter) and its ADR-0001 citation is apt — the ADR does place sandboxing and tool permissioning on the harness. Every non-documentation scenario has a test whose docstring names it. The dry run touches no project root, journal or snapshot, and `_parse_verdict(preserve_output=False)` keeps it from leaving a rejected-output file behind; the three dry-run tests assert that. `tasks.md` is fully ticked and each tick corresponds to work in the diff.

**F-DRY-RUN-MISLABELS-LAUNCH-FAILURE** — `src/agentmarshal/cli.py:584` prints every `ReviewLaunchError` from the dry-run path as `dry run could not parse reviewer verdict: ...`, but `dry_run_review` raises that same type for failures that are not parse failures: an unset `AGENTMARSHAL_REVIEWER_CMD` (`review.py:196`), an unparseable or empty template, `cannot run reviewer:` from a missing executable (`review.py:253`), a non-zero reviewer exit (`review.py:256`), and the unsupported-placeholder refusal this very task adds (`review.py:212`). An operator who has configured nothing runs `agentmarshal review --dry-run` and is told the verdict could not be parsed, followed by instructions about an environment variable — the wrong half of the problem, in the one command whose purpose is to diagnose the configuration before the first real review. No test covers the dry run's launch-failure path.

**F-EMPTY-PLACEHOLDER-NOT-NAMED** — `src/agentmarshal/journal/review.py:168` treats the auto-numbered field `{}` as unsupported (its field name parses as the empty string, which is not in `{"model", "prompt_file"}`) and returns `""`, so `review.py:212` emits `AGENTMARSHAL_REVIEWER_CMD has an unsupported placeholder: ` with nothing after the colon. `{}` is an ordinary shell idiom to carry into a command template, and the spec requirement "A rejected placeholder is named" makes naming the token mandatory, not best-effort. The parametrized test at `tests/test_review_launcher.py:313` covers `unsupported` and `0`, both of which have a non-empty name, so the hole is untested.

**A-DRY-RUN-CWD-NOT-A-SNAPSHOT** — `review.py:389` runs the command with `cwd` set to the dry run's own temporary directory, which holds only `review-prompt.txt`, while a recorded review runs it in `temporary_root/snapshot` populated from the reviewed tree. A relative-path reviewer command — precisely the configuration proposal 015 reports adopters losing launches to — therefore fails in a dry run while succeeding in a real one. `design.md` records the decision not to build a snapshot but not this consequence, and nothing tells the operator the dry run cannot vouch for that half of the contract.

**A-DRY-RUN-MODEL-DEFAULT** — `src/agentmarshal/cli.py:582` substitutes the literal string `dry-run` for `{model}` when `--model` is absent. Against a real vendor template such as `codex exec --model {model} -`, the plain `review --dry-run` then fails on an unknown model name, reporting a problem the operator's configuration does not have. Neither `--model`'s help nor `--dry-run`'s says the two go together.

**A-QUICKSTART-OMITS-DRY-RUN** — `docs/quickstart.md:46` is the section an operator reads to configure `AGENTMARSHAL_REVIEWER_CMD`, and it is the natural place to say the configuration can be exercised without recording; it does not mention `--dry-run`, which is discoverable only from `review --help`.

**A-ARCHIVED-PROPOSAL-LINK-DEPTH** — `openspec/changes/archive/2026-09-17-document-the-reviewer-contract/proposal.md:5` links proposal 015 as `../../../docs/proposals/015-reviewer-command-contract-undocumented.md`, which resolves to `openspec/docs/proposals/...` from a directory four levels deep. The sibling change archived the same day, `openspec/changes/archive/2026-09-17-render-amendment-history/proposal.md:3`, uses `../../../../`; this file reads as written for the un-archived path and moved without adjusting the depth, so the link to the report that motivates the change is dead.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "fcbd305bf907ee151b2a6408729c0bc589ff0dd0", "verdict": "changes_required", "findings": ["F-DRY-RUN-MISLABELS-LAUNCH-FAILURE", "F-EMPTY-PLACEHOLDER-NOT-NAMED"], "advisory_findings": ["A-DRY-RUN-CWD-NOT-A-SNAPSHOT", "A-DRY-RUN-MODEL-DEFAULT", "A-QUICKSTART-OMITS-DRY-RUN", "A-ARCHIVED-PROPOSAL-LINK-DEPTH"]}
AGENTMARSHAL_VERDICT_END
