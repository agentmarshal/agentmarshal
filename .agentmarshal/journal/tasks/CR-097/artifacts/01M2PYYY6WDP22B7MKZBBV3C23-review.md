Разобрал диф целиком: прочитал `review.py`, `cli.py`, оба spec-файла, `design.md`, `tasks.md`, quickstart, ADR-0001/0005, proposal 015 и новые тесты; прогнать pytest в песочнице не дали (команда потребовала approval), поэтому тесты проверены чтением — мысленный прогон каждого нового теста сходится.

По acceptance criteria всё закрыто: сценарии спеки покрыты тестами с именующими докстрингами, отступление про snapshot HEAD записано в `design.md`, dry run не трогает журнал и не требует ни задачи, ни коммита, промпт строится единым `_review_prompt`/`_REVIEW_PROMPT` (пинованный 0.3.0-тест не тронут), отказ по плейсхолдеру называет токен и проверяется тестом без сверки всей фразы, quickstart говорит все четыре вещи, чекбоксы проставлены. Блокирующих дефектов не нашёл; попутно диф чинит скрытый краш — `{0}` раньше давал непойманный `IndexError`.

ADV-001: справка `--task/--base/--role/--vendor/--email/--commit` в `src/agentmarshal/cli.py:158-176` говорит только «required unless --dry-run», то есть читается как «необязателен при --dry-run», тогда как `_run_review` эти флаги при dry run отвергает с кодом 1; комментарий над ними утверждает «argparse cannot say that, so the help does», но справка этого не говорит ни для одного из пяти флагов, кроме `--reviewed-finding`.

ADV-002: `dry_run_review` в `src/agentmarshal/journal/review.py:442` проверяет только парсинг и совпадение коммита, но не словарь вердиктов и не остальные правила записи, поэтому команда, печатающая `"verdict": "lgtm"` или пустые `findings` при `blocked`, объявляется рабочей и всё равно упадёт на первом настоящем ревью — ровно тот отказ, который комментарий в `review.py:578-580` называет самым частым на практике; `REVIEW_VERDICTS` уже импортирован в модуль.

ADV-003: параметр `preserve_output`, протянутый через `_reject` и `_parse_verdict` (`src/agentmarshal/journal/review.py:299-320`), избыточен — dry run выключает сохранение и тут же сам сохраняет тот же вывод в `review.py:441-452`, получая почти ту же фразу, так что новая ветка в `_reject` и замыкание `reject` существуют ради формулировки сообщения.

ADV-004: отказ по несовпадению коммита в `src/agentmarshal/journal/review.py:456-460` — единственный путь провала dry run, который выбрасывает вывод ревьюера, не сохранив и не назвав его, в отличие от соседних ошибок парсинга и от аналогичной проверки записываемого пути (`review.py:554-557`), которая идёт через `_reject`.

ADV-005: контракт и список documents указывают на `openspec/changes/document-the-reviewer-contract/`, которого в ревьюируемом коммите нет — работа лежит в `openspec/changes/archive/2026-09-17-document-the-reviewer-contract/`; гейт проходит, потому что тронут `openspec/specs/reviewer-adapter/spec.md`, но названный ревьюеру документ не открывается.

ADV-006: `tests/test_cli.py:16` ищет подстроку «records nothing» в перенесённой argparse-справке; строка помещается только при ширине терминала около 80 и разорвётся на узком терминале или при выставленном `COLUMNS`.

```
AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "e965819ed4875a8a3020abb7a262da779b4a96c3", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-001", "ADV-002", "ADV-003", "ADV-004", "ADV-005", "ADV-006"]}
AGENTMARSHAL_VERDICT_END
```
