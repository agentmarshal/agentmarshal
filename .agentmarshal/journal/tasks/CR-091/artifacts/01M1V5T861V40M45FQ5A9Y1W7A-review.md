I reviewed the diff statically against the contract, the archived spec/design, and the surrounding code. I could not execute the test suite (`uv run pytest` required an approval I don't have as a read-only reviewer), so the verification below is by reading: I traced each spec scenario to its test, and each acceptance criterion to the code that satisfies it.

What holds up:

- **Criterion 1** — all seven scenarios in `openspec/specs/review-evidence/spec.md` have a test whose docstring names them (`test_model_review_path_keeps_its_output`, `test_human_review_path_attaches_prose`, `test_rejected_verdict_still_keeps_the_prose`, `test_gate_refuses_a_modified_artifact`, `test_artifacts_hash_is_checked_where_its_record_is_validated`, `test_status_names_the_prose`, `test_old_journal_reads_as_before`). The implementation follows design.md's four decisions: location, pin-not-embed, write-before-record, append-only extension.
- **Criterion 2** — `write_artifact` is the single writer; `submit_review` pre-generates the ULID, pre-validates the record, writes the artifact, then pins it and passes the same id to `write_record`. Ordering is correct. Bytes are written through `_create_exclusive().buffer`, so the O_NOFOLLOW/O_EXCL guarantee is kept and binary prose round-trips untranslated (the `\xff` in `test_human_review_path_attaches_prose` exercises this). The finding path never writes artifact bytes — the operator supplies `REF=SHA256` — so there is nothing there to route through the helper.
- **Criterion 3** — `_is_append_only_evidence_path` covers both lanes (`changes_with_status` for embedded, `git status` plus `git log -m --first-parent --diff-filter=MDR` for sidecar), and renames still decompose into D+A, so moving an artifact out of `artifacts/` is caught as a deletion. `_review_artifact_failures` refuses a ref outside the task's own `artifacts/`, an absolute ref, a `..` component, a missing or symlinked file, and a hash mismatch, naming the record id in every message.
- **Criterion 4** — the transcript check labels are untouched, and no existing launcher or gate test was edited.
- **Criteria 5 and 6** — `openspec/changes/keep-review-prose/` is gone from the tree, the archive holds it under `2026-09-06-keep-review-prose/`, the baseline spec is in place, every tasks.md box is ticked, and both doc sentences landed. I could not run `openspec validate` (no CLI in the environment), so that half of criterion 5 is unverified rather than confirmed.

The advisory findings follow.

**ORPHANED-ARTIFACT-ON-RECORD-REFUSAL** — `src/agentmarshal/journal/submit_review.py:81-85` writes the prose artifact before `write_record`, but `write_record` applies one check the pre-validation at line 77 does not: the `_SCHEMA_4_FIELDS` binding check that `reviewed_finding` names a finding record in the same task. So `submit-review --reviewed-finding <well-formed-but-unknown-ULID> --prose f.md` writes `tasks/<id>/artifacts/<ulid>-review.md`, then fails and writes no record — leaving prose from a refused verdict inside a directory this same change just made append-only. The comment at line 74 claims the pre-check refuses malformed verdicts "before creating their durable artifact", and it does for verdict shape; it does not for the binding.

**REDUNDANT-TEMP-COPY-ON-SUCCESS** — `src/agentmarshal/journal/review.py:438-443` still writes a best-effort temp copy of the prose whenever a recorded review names findings, and `src/agentmarshal/cli.py:600-606` prints only that ephemeral path to the operator, under a comment stating "The record names findings by id only. Point at the reasoning on stderr." After this change the record does name the reasoning, durably and by hash, and the operator is never told where. proposal.md's Why is precisely that the output "survives only as a best-effort file under the system temp directory"; the success path still behaves that way in what it reports.

**STALE-ROADMAP-PARAGRAPH** — `docs/overview.md:155-159` still reads "In 0.3.0 the policy parser exists but no policy-driven writer or private store retains those artifacts. Reviewer output with findings is kept separately in a best-effort temporary file; that is not policy-governed durable capture," listing "full review ... text" as the thing not yet retained. The version is still 0.3.0 (`pyproject.toml:3`), so this now contradicts the Record entry fifty lines above it, which the same diff edited to say prose is pinned by SHA-256.

**HELPER-TRUSTS-TASK-ID** — `src/agentmarshal/journal/artifacts.py:50-52` carefully rejects a traversal in `name` (single path component, no control characters) but passes `task_id` straight into `_prepare_record_directory`, which validates no task id and calls `mkdir` on each segment. Both current callers validate upstream via `load_task_status`, so nothing is exploitable today; the asymmetry is a trap for the next caller of a module-level helper whose whole purpose is a safe exclusive write.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "bb447489e22fdda0ab33d982e90fe6663e1b1364", "verdict": "approved", "findings": [], "advisory_findings": ["ORPHANED-ARTIFACT-ON-RECORD-REFUSAL", "REDUNDANT-TEMP-COPY-ON-SUCCESS", "STALE-ROADMAP-PARAGRAPH", "HELPER-TRUSTS-TASK-ID"]}
AGENTMARSHAL_VERDICT_END
