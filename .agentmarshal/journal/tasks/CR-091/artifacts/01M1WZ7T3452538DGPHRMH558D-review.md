I reviewed the diff statically against the contract, the spec, and the surrounding code. I could not execute the test suite (`python3 -m pytest` required approval that wasn't granted), so the "tests pass unmodified" clause of acceptance criterion 4 is unverified by execution; I checked it by reading the existing assertions instead — `test_a_clean_approval_keeps_nothing_and_says_nothing` only asserts `"kept at" not in captured.err`, which the new `reviewer prose pinned:` line does not trip, and `test_embedded_diff_lane_transcript_matches_published_030_byte_for_byte` goes through `_approve`, which submits no prose, so no artifact enters that transcript.

What I confirmed holds: all seven spec scenarios have a test whose docstring names them; `write_artifact` is the single writer and `finding --artifact REF=SHA256` never writes bytes, so criterion 2's "where they are written by this tool" clause has nothing to route; `_is_append_only_evidence_path` is a strict superset of `_is_record_path`, so record behaviour is byte-identical and the sidecar branch resets it correctly; `validate`'s `resolved != lexical` symlink check is right in both embedded and sidecar placements, because `Placement.journal_root` is always `<project_root>/.agentmarshal/journal`; and the archive/baseline split under `openspec/` is in place with `openspec/changes/keep-review-prose/` gone.

Three advisory items below. None of them blocks.

**A-001** — `src/agentmarshal/journal/submit_review.py:75-98` writes the artifact before the record and guards only two of `write_record`'s refusal modes with a hand-copied pre-flight: the `reviewed_finding` error string at line 91-94 is a verbatim duplicate of `records.py:691`, keyed on a manually re-derived finding set rather than on `_SCHEMA_4_FIELDS`. Any other refusal inside `write_record` — a ULID collision at `_create_exclusive`, `resolve_recorded_by` raising while reading the actors table, or a validation rule added later — leaves an unreferenced file in a directory the gate has just been taught to treat as immutable. Once such an orphan is committed as an addition it can never be deleted, and `validate` only walks pins forward from records, so it never reports a file that no record cites. The ordering itself is design.md's decision and the implementation follows it; the residual window is what design.md does not address.

**A-002** — `src/agentmarshal/journal/review.py:446-451` still copies the reviewer's prose a second time into `tempfile.mkstemp` for every accepted verdict that names a finding, now that the same bytes are durably pinned in the journal. The `SubmittedReview` docstring rewritten at `submit_review.py:28-33` justifies this as "for compatibility with the rejected-verdict path", but the rejected path reaches `_preserve_output` through `_reject` (`review.py:234-241`) and never touches this branch, so the justification does not describe the code. The copy is never cleaned up and lands outside the journal, which is the one place the contract's threat model says publication is decided (ADR-0008). Relatedly, `docs/overview.md` deleted the sentence that documented this temp file while the CLI still prints `reviewer output kept at ...`, so the behaviour is now live but undocumented.

**A-003** — `src/agentmarshal/journal/report.py:65-69` sums artifacts across every review record of a task and `format_report` prints that total on the task line, but the spec scenario "status names the prose" says the review line reports how many *it* carries "and `report` shows the same". For a task with two reviews carrying one artifact each, `status` prints `artifacts=1` twice while `report` prints `artifacts=2`. `test_status_names_the_prose` uses a single review, so the totals coincide and the test cannot discriminate. `report` has no per-record line, so a per-record count is arguably not expressible there — but design.md records no decision about display and therefore no departure either.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "8daae5ee5d76c369b8ea2778d651dadfbddec17d", "verdict": "approved", "findings": [], "advisory_findings": ["A-001", "A-002", "A-003"]}
AGENTMARSHAL_VERDICT_END
