Проверил diff против контракта, spec'ов, ADR-0005 и кода в снапшоте.

**Что сходится.** Оба новых сценария из `openspec/changes/archive/2026-09-18-bounded-leak-scan-output/specs/leak-scan/spec.md` закрыты тестами, чьи docstring их называют: `tests/test_gate.py:1805` и `tests/test_leak_scan.py:63`. Остальные шесть сценариев MODIFIED-требования уже были запинены в CR-100 и продолжают держаться (`tests/test_capture.py:336,489,354,375,242`, `tests/test_gate.py:1774`). Текст требования в дельте байт-в-байт совпадает с опубликованным `openspec/specs/leak-scan/spec.md` — новый абзац про bound применён ровно один раз. Рендерер остался один: `render_leak_hits` (`src/agentmarshal/journal/capture.py:324`) вызывается из `src/agentmarshal/cli.py:1309` с `limit=None` и из `src/agentmarshal/journal/gate.py:1187` с `_LEAK_HIT_RENDER_LIMIT`; форма хита живёт только в рендерере, gate держит только число, а `test_the_merge_boundary_reports_the_same_detail` по-прежнему пинит обе строки в одном тесте, так что разъехаться они не могут. `_RENDER_LIMIT` из `capture.py` убран без остатков. `LaunchedReview` добавлен и в импорт, и в `__all__` (алфавитный порядок сохранён, строка ровно 88 символов — в лимит ruff укладывается), тест `tests/test_journal.py:94` пинит идентичность объекта. Все одиннадцать тронутых путей внутри scope, non-goals не нарушены: флага для bound нет, число осталось двадцать, scan остался advisory. Противоречия с ADR-0005 нет — решение про «advisory, не block-on-leak» и про то, что вывод никогда не несёт сам секрет, эта задача не трогает. Архивная директория создана по той же конвенции, что и соседние (`.openspec.yaml` со `schema: spec-driven` и `created`), как и в CR-100.

Ниже — одно необязывающее замечание.

**ADV-SIDECAR-DOC-STATES-THE-BOUND-UNCONDITIONALLY** — `docs/sidecar.md:249-252` описывает bound без условия: «The line is one line of a transcript, so it shows the first twenty hits and ends `, and N more not shown`». Читается как «всегда», тогда как gate дописывает суффикс только когда хитов больше двадцати — и собственный тест gate'а, на соответствие которому ссылается acceptance-критерий 4, пинит строку с одним хитом *без* этого суффикса (`tests/test_gate.py:1796`). И `proposal.md`, и `design.md` формулировку обусловливают («unchanged for any candidate with twenty hits or fewer, and for more it says what it left out») — в документ условие не доехало. Шапка проекта в самом же `CHANGELOG.md` («describes what it does and not what it intends to do») задаёт для такой прозы более высокую планку. Не блокирует: описанная форма строки — та самая, что печатает код, расходится только область применения.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "4a97254fe0fbbc36f20d24e1900204bae4ea7abd", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-SIDECAR-DOC-STATES-THE-BOUND-UNCONDITIONALLY"]}
AGENTMARSHAL_VERDICT_END
