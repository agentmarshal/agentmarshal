I reviewed the launcher, the shared resolver, the gate refactor, the CLI wiring, the prompts, the specs and the docs against the contract's five acceptance criteria.

**What holds up.** The commit prompt refactor is genuinely byte-preserving: `_prose_instruction()` reproduces the old literal including the `\`-continued "one line of prose" break, and `_verdict_protocol("reviewed_commit", "the exact reviewed commit SHA")` reproduces the old block exactly, with `_named_contract_material` rendering identically when `preamble` is empty (`src/agentmarshal/journal/review.py:161`, `:172`, `:127`). The gate transcript is also unchanged: `finding_reviewer_identity_refusal` returns the same two strings the old inline `check` calls passed, and the `or` fallback at `src/agentmarshal/journal/gate.py:236` reproduces the PASS wording (`gate.py:109`). `_artifact_path` is gone from `gate.py` and both callers hold the one function object in `artifacts.py:17`. All thirteen `findings-review` scenarios and both `reviewer-adapter` scenarios have a test whose docstring names them, and the design's one departure (refusal wording) is recorded as a departure. The injection defence is sound — `splitlines()` is the same function the parser uses, so no content line can reach column zero unprefixed — and the unprefixed headings are safe because `records.py:468` and `:479` already reject control characters in a finding's summary and refs. Binding exclusivity, the `--base` refusal, the sidecar `journal_root.parents[1]` derivation, the `ExtensionManifestMissing`-before-`ExtensionManifestError` ordering, and `submit_review`'s argument order all check out. I could not execute the test suite in this sandbox, so the pass claims above are from reading, not from a run.

One advisory finding follows.

ADV-0001 — `docs/quickstart.md:48` tells operators that a relative path in `AGENTMARSHAL_REVIEWER_CMD` resolves inside the snapshot, and `src/agentmarshal/journal/review.py:901` makes that snapshot, for a finding review, a directory containing nothing but the verified artifacts; a command configured as a relative path therefore works for commit reviews and dies with a bare "cannot run reviewer" on finding reviews, which is exactly what the reviewer-adapter scenario "an operator reads that documentation to configure one command for both kinds of review" is meant to prevent. The design note at `openspec/changes/archive/2026-09-18-review-binds-to-a-finding/design.md` carries the inverted rationale behind it — it says the finding snapshot exists "so a relative path in the reviewer command resolves", when what it actually makes resolve is the relative *artifact* paths the prompt names.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "3a283df97abf557fbbaac9130a3921ea75d13884", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-0001"]}
AGENTMARSHAL_VERDICT_END
