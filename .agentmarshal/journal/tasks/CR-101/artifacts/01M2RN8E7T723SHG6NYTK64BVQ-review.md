Review проведён по коммиту `3a283df97abf557fbbaac9130a3921ea75d13884`. Я прочитал `review.py`, `gate.py`, `artifacts.py`, `cli.py`, обе спеки, оба doc-файла и тесты; прогнать pytest в этой песочнице не дали (команда требует подтверждения), поэтому проверка тестов — чтение кода и сверка ожиданий вручную, о чём говорю прямо.

**Что сошлось.** Pinned-промпт commit-пути воспроизводится байт в байт: `_prose_instruction()` и `_verdict_protocol("reviewed_commit", …)` дают ровно те же строки, что были в литеральном шаблоне, включая перенос `print one line of \`+`prose`. Транскрипт findings-lane в `gate.py` тоже стабилен — при `launching=False` `finding_reviewer_identity_refusal` возвращает те же две формулировки, что `check()` печатал раньше, и в успешном случае `check(True, …)` получает прежний label. Резолвер `artifact_path` действительно один (`artifacts.py:17`), `_artifact_path` из `gate.py` убран, тест на тождество объектов это фиксирует. Все сценарии обеих спек имеют тест с докстрингом, который их называет. Защита от инъекции вердикта корректна: префикс ставится по `str.splitlines()` — тому же разбиению, которым `_parse_verdict` читает вывод, — поэтому одиночный `\r`, `\x85`, `\u2028` тоже не выносят сентинел в нулевую колонку; а `summary` и `ref` уже отвергаются `_reject_control_characters`, так что через них строку не добавить. `reviewed_contract` при `reviewed_finding` даёт schema 5 и проходит валидацию записи; ADR-0011 findings-lane явно оставил открытой, так что решение design.md ей не противоречит.

Блокирующих дефектов не нашёл. Два замечания — необязательных.

ADV-001 — в `src/agentmarshal/cli.py:184` help для `--base` переписан на «required for a commit review; refused with --reviewed-finding» и потерял прежнее «required unless --dry-run». Сам dry-run по-прежнему отвергает `--base` (флаг остался в списке `ignored`), так что справка теперь описывает флаг уже, чем код: оператор, читающий её, не узнает, что `--base` несовместим с `--dry-run`. У соседнего `--commit` этот факт сохранён, и расхождение между двумя строками справки видно глазом.

ADV-002 — в `docs/sidecar.md:415` runbook findings-lane теперь запускает `agentmarshal review --reviewed-finding`, но внутри блока нигде не сказано, что для этого должен быть выставлен `AGENTMARSHAL_REVIEWER_CMD`; без него команда падает с «no reviewer command configured». Зависимость проговорена только контрапозитивно, в заголовке абзаца ниже — «Human alternative (no reviewer command configured)». Адоптер, копирующий блок целиком, упирается в ошибку — ровно тот класс расхождения кода и рунбука, из-за которого `docs/sidecar.md` и попал в scope этой задачи.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "3a283df97abf557fbbaac9130a3921ea75d13884", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-001", "ADV-002"]}
AGENTMARSHAL_VERDICT_END
