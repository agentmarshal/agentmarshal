Review complete. The launcher itself is sound: the commit prompt is reproduced byte for byte by the shared-helper split, the gate's findings-lane transcript is preserved exactly (`finding_reviewer_identity_refusal` returns the old pass-case wording and only diverges behind `launching=True`), the resolver now exists once in `artifacts.py:17`, the three pre-run refusals and both artifact refusals are implemented and ordered before `_run_reviewer`, the sentinel prefixing genuinely defeats an embedded verdict block, and all eleven spec scenarios have a test whose docstring names them. What follows are the gaps.

**doc-reviewer-contract-stale** (blocking) — `docs/quickstart.md` is where the reviewer command's contract is published, and it now describes only half the launcher. Line 47-48 states "the command starts with its working directory set to a metadata-free snapshot of the reviewed commit"; a finding review's snapshot is a directory of verified artifact bytes (`review.py:745-765`), not a commit tree. Worse, lines 69-80 state that the command "must" emit `reviewed_commit` — for a finding review `review.py:872` refuses exactly that, after the run is already spent. An adopter whose wrapper hardcodes the documented key per quickstart burns a reviewer run on every finding review, which is the class of waste the design's pre-run refusals exist to prevent. `openspec/specs/reviewer-adapter/spec.md:11-18` requires this contract to be documented and is likewise unamended. Neither file is in CR-101's scope, so closing this needs a contract amendment — the same move the task already made for `docs/sidecar.md`, and for the same stated reason ("Shipping a path in one release and its documentation in another is how the adopter batch's finding 016 happened").

**snapshot-escape-refusal-unspecified** (advisory) — `review.py:757-763` adds a fourth pre-run refusal that the spec does not list. A reference whose spelling contains a cancelling `..` (e.g. `../<project-dir-name>/evidence/x.md`) resolves inside the project, so `artifact_path` (`artifacts.py:17`) accepts it and the findings gate verifies and passes it, but joining that spelling to the snapshot root resolves outside it and the launcher refuses with "finding artifact reference escapes the snapshot". That is a finding the lane will verify and the automated channel can never review — the exact deadlock CR-101 exists to remove. Contrived to reach (refs are recorded verbatim, `cli.py:956`), and it fails safe, but it is a divergence from the one-resolver decision.

**no-identity-branch-untested** (advisory) — the independence scenario covers two conditions ("is one of the finding recorder's declared git identities, **or** the recorder resolves to no git identity at all"). `gate.py:125` returns a distinct refusal for the second, reachable from the launcher, but `test_finding_review_refuses_the_recorder_before_running` only asserts the first ("declared reviewer identity is not independent"). Half the scenario the contract says must be demonstrated has no test on the launcher path.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "625ad0939d5b0dcd7ad4979850a3634df95b9057", "verdict": "changes_required", "findings": ["doc-reviewer-contract-stale"], "advisory_findings": ["snapshot-escape-refusal-unspecified", "no-identity-branch-untested"]}
AGENTMARSHAL_VERDICT_END
