I reviewed the launcher, the shared resolver extraction, the gate refactor, the CLI wiring, the specs, and the tests. The core mechanics are sound — the commit prompt is reproduced byte-for-byte by the new shared helpers, the gate transcript strings are preserved exactly through `finding_reviewer_identity_refusal`, the sentinel prefixing works as specified, and every scenario in the new spec has a test whose docstring names it. Four defects remain.

CR101-001: `docs/sidecar.md:414-420` now shows `agentmarshal review --reviewed-finding` and `agentmarshal submit-review --reviewed-finding` as two consecutive lines of one runbook block, with no prose distinguishing them; an adopter who runs the block top to bottom records two review records for the same finding, and because `run_findings_gate` judges `reviews[-1]` (`src/agentmarshal/journal/gate.py:184`) the hand-recorded human verdict is the one the gate reads — the launched review it was supposed to replace is silently ignored. The contract amendment put this file in scope specifically to route the runbook to the automated path, and the routing was added rather than changed.

CR101-002: `_extract_finding_snapshot` at `src/agentmarshal/journal/review.py:744` computes `snapshot / artifact.reference`, and pathlib discards `snapshot` when the reference is absolute, so an absolute artifact reference under the project root — which `artifact_path` at `src/agentmarshal/journal/artifacts.py:26` explicitly supports and the findings gate happily verifies — fails the confinement check and aborts the launch with "finding artifact reference escapes the snapshot". The reviewer never runs on a finding the gate calls fully verified, and the `# pragma: no cover - guarded by resolver` comment on `review.py:747` asserts the branch is unreachable, which is why nothing tests it.

CR101-003 (advisory): `tests/test_gate.py:257` asserts `not hasattr(gate_module, "actor_git_identities")`, a name that has never existed in `gate.py` — the real function is `_actor_git_identities` and it is still defined there. Task 2.4's stated verification is that `_artifact_path` is gone from `gate.py`; this assertion can never fail and guards nothing against a private resolver copy reappearing.

CR101-004 (advisory): `_extract_finding_snapshot` at `src/agentmarshal/journal/review.py:733` takes a `project_root` parameter its body never reads, and `_VerifiedArtifact.path` (`review.py:118`) is populated at `review.py:718` but never read by any caller — both are dead weight carried into a file that otherwise justifies every field it keeps.

CR101-005 (advisory): the sidecar command table at `docs/sidecar.md:397` still lists `review` under "Unchanged, writing to the sidecar and reading host git facts where needed", but `src/agentmarshal/cli.py:710` now passes `require_host=args.reviewed_finding is None`, so a finding review deliberately succeeds against an unreachable host — the one sidecar-specific behaviour change in this diff, undocumented in the sidecar document this task put in scope.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "c9c7d0effa9ef5418084e672c86bdd4ebd97b8fe", "verdict": "changes_required", "findings": ["CR101-001", "CR101-002"], "advisory_findings": ["CR101-003", "CR101-004", "CR101-005"]}
AGENTMARSHAL_VERDICT_END
