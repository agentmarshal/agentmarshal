I reviewed the diff statically against the contract, the two named decisions, and both spec documents. Note up front: this sandbox refused to execute Python, so I could not run the test suite — my reading of test/implementation agreement below is static, and the acceptance criterion "the pinned commit-prompt test and the byte-for-byte gate transcript tests pass" is asserted from code inspection, not from a run.

**What I checked and found sound.** The commit prompt is reconstructed from `_prose_instruction()` and `_verdict_protocol("reviewed_commit", …)` byte-for-byte, including the awkward `"of " + "prose\n"` seam from the old line continuation, so `tests/test_review_launcher.py:1980` should still match its unmodified expectation. The findings-gate identity refactor (`src/agentmarshal/journal/gate.py:109`) reproduces the old transcript in all three branches — empty recorder identities, matching identity, and the passing case — so the lane's published wording is preserved and the launcher wording is opt-in via `launching=True`, exactly as the recorded departure in `design.md` says. `_artifact_path` is genuinely gone from `gate.py` and both callers now read `agentmarshal.journal.artifacts.artifact_path`. The injection defense holds: artifact content is prefixed per `str.splitlines()` boundary — the same boundary set `_parse_verdict` uses, which is why `\r`, `\x0b`, `\x85` and `\u2028` cannot slip a sentinel to column zero — and the other new unprefixed prompt inputs (the finding summary, artifact refs) are already rejected by `_reject_control_characters` at `records.py:504`, which refuses every Unicode separator and control character, not just `\n`. Record schema selection reaches 5 with `reviewed_finding` + `reviewed_contract` + `artifacts`, and `_validate_record` permits that combination. Every scenario in both spec deltas has a test whose docstring names it, every changed file is inside the declared scope, and no `agentmarshal.journal` import cycle is introduced.

The four items below are non-blocking.

ADV-001: `openspec/changes/archive/2026-09-18-review-binds-to-a-finding/design.md:54` sends the reader to "the Non-Goals below" for why no size threshold is invented, but Non-Goals is at line 24, above Decisions — and that section lists nothing about size or count limits. The statement it points at lives in the Risks section at line 127 and in the task contract's own Non-Goals, so the cross-reference is wrong in both direction and target, in a document the contract names.

ADV-002: `src/agentmarshal/journal/review.py:271-276` tells the reviewer a binary artifact's content is not embedded and gives its hash and size, but nothing in `_FINDING_REVIEW_PROMPT` says the verified files are sitting in the reviewer's own working directory. The "Snapshot path" line only appears when the reference and the snapshot path differ (`review.py:265`), so for the ordinary case of a relative reference to a PDF the launched reviewer is handed a name, no bytes, and no stated way to reach them. A commit review never needs this because the diff is self-contained.

ADV-003: `src/agentmarshal/journal/review.py:29` makes the launcher import the merge gate to reach `finding_reviewer_identity_refusal`. The same change moved the other rule shared by these two modules, `artifact_path`, *out* of `gate.py` into the neutral `artifacts.py` on the stated reasoning that a shared decision needs one home — and then gave the identity rule the opposite treatment, so importing the launcher now pulls in the gate's leak scan, capture and git machinery.

ADV-004: `src/agentmarshal/journal/review.py:890-954` repeats the tail of `launch_review` at `review.py:1007-1119` — temporary directory, prompt file, `_run_reviewer`, `_keep_diagnostics`, decode, parse, `_with_diagnostics`, `submit_review`, the `artifact_ref is not None` branch, and the `LaunchedReview` construction. The design's risk register anticipates the two *prompts* drifting and pins both with byte-for-byte tests; this second duplicated region has no equivalent guard. The dead `raw_output = b""` / `reviewer_output = ""` pre-initializations at `review.py:890-891` are carried over with it.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "9dfad34c7a8129e24315db3dc98bba4ae9ff622f", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-001", "ADV-002", "ADV-003", "ADV-004"]}
AGENTMARSHAL_VERDICT_END
