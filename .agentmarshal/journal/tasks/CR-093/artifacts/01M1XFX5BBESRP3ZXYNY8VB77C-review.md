Разобрал diff, прочитал `gate.py` целиком вокруг изменения, все четыре новых теста, обе копии спеки и структуру архива. Блокирующих дефектов не нашёл — реализация совпадает с design.md, три поведенческих теста действительно падают при откате на `git diff --name-only`, спека заархивирована по образцу предыдущей. Ниже — advisory.

**ADV-DUPLICATE-TRANSCRIPT-TEST** — `tests/test_gate.py:309`: новый `test_candidate_without_renames_prints_the_transcript_it_printed_before` дословно повторяет уже существующий `test_embedded_diff_lane_transcript_matches_published_030_byte_for_byte` (`tests/test_gate.py:270`) — тот же `_gate_repo(["src/"])`, тот же `_implement("src/module.py")`, те же аргументы и те же три assert'а, минус последняя строка `assert current.out.endswith("gate: passed\n")`. То есть это строго более слабая копия, которая вдобавок вторично запускает дорогой subprocess с релизным 0.3.0. Сценарий можно было закрыть, дописав docstring существующему тесту.

**ADV-SCENARIO4-NOT-MUTATION-SENSITIVE** — `tests/test_gate.py:314`: acceptance требует, чтобы каждый сценарный тест падал при возврате scope-проверки на `git diff --name-only`. Тест сценария «a candidate without renames prints the transcript it printed before» по построению не падает — в его range нет rename, обе реализации дают один и тот же `changed`. Это противоречие внутри самого контракта (сценарий описывает отсутствие изменения), а не ошибка исполнителя, но критерий формально не выполнен для одного из четырёх сценариев.

**ADV-TASKS-VERIFY-COMMAND-WRONG** — `openspec/changes/archive/2026-09-07-close-rename-scope-hole/tasks.md:3`: галочка 1.1 стоит, но её verify-рецепт неверен как написан. `grep -n "name-only" src/agentmarshal/journal/gate.py` даёт четыре попадания, а не «только base-tree `ls-tree`»: `gate.py:376` (`git log --name-only` в `_sidecar_history_tampering`), `gate.py:529` (`ls-tree` по `_PROJECT_FILE`), `gate.py:610` (base tree) и `gate.py:859` (candidate tree). Содержательное утверждение задачи верно — `_changed_paths` удалён и `git diff --name-only` из gate ушёл, — но команду проверки нельзя выполнить и получить заявленный результат.

**ADV-STALE-NAME-ONLY-DOCSTRING** — `src/agentmarshal/journal/open_task.py:90`: docstring `scope_warnings` по-прежнему утверждает «The gate compares scope entries against paths from ``git diff --name-only``». После этого изменения gate сравнивает scope с путями из `git diff --name-status`. Суть предупреждения (git перечисляет файлы, не директории) осталась верной, устарела только названная команда. Файл вне scope контракта, поэтому правка здесь была бы нарушением scope — фиксирую как долг.

**ADV-NAMED-DOCUMENTS-UNTOUCHED** — `src/agentmarshal/journal/gate.py:844`: контракт CR-093 объявляет `documents = ["openspec/changes/close-rename-scope-hole/"]`, а кандидат по требованию acceptance №4 архивирует изменение — все файлы легли под `openspec/changes/archive/2026-09-07-close-rename-scope-hole/`, и по пути `openspec/changes/close-rename-scope-hole/` в дереве не остаётся ничего. `scope_covers(("openspec/changes/close-rename-scope-hole/",), "openspec/changes/archive/2026-09-07-close-rename-scope-hole/design.md")` возвращает False, поэтому `touched_documents` пуст и собственный gate этого кандидата напечатает `FAIL: named documents untouched: openspec/changes/close-rename-scope-hole/`. Scope-проверка при этом проходит (`openspec/changes/archive/` и `openspec/specs/scope-enforcement/` в scope есть). Diff тут ни в чём не виноват — противоречие внутри контракта между `documents` и требованием архивировать в том же кандидате; чинить надо контракт, а не изменение.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "b6ceb9099ace7a41345b31a548d0e189ff016d1a", "verdict": "approved", "findings": [], "advisory_findings": ["ADV-DUPLICATE-TRANSCRIPT-TEST", "ADV-SCENARIO4-NOT-MUTATION-SENSITIVE", "ADV-TASKS-VERIFY-COMMAND-WRONG", "ADV-STALE-NAME-ONLY-DOCSTRING", "ADV-NAMED-DOCUMENTS-UNTOUCHED"]}
AGENTMARSHAL_VERDICT_END
