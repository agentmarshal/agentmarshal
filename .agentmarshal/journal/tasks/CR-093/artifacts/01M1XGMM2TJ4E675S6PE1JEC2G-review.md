Проверил изменение статически: прочитал `gate.py` целиком вокруг изменённых мест, новые тесты, spec/design/proposal/tasks, ADR-0010 и все места в репозитории, где ещё могло остаться второе перечисление путей.

Что сходится:

- `_changed_paths` действительно удалён; `grep name-only` по `src/` оставляет только `ls-tree` и `log --name-only` (`gate.py:376`, `529`, `610`, `859`) — ни одного `diff`-перечисления.
- Единственный список: `changed = list(dict.fromkeys(path for _status, path in changes_with_status))` (`gate.py:604`), и все три читателя берут его — пустой диапазон (`605`), выбор полосы `journal_only` (`655`), scope-проверка (`796`). Порядок пар из `--name-status -z` для `R` — сначала `D` источника, потом `A` назначения, так что источник переименования и правда попадает во все три.
- Поведение для кандидата без переименований не меняется: `--name-only` и дедуплицированные пути `--name-status` совпадают, `outside` по-прежнему сортируется перед печатью, формулировки PASS/FAIL не тронуты — байт-в-байт транскрипт 0.3.0 должен остаться прежним.
- Деградация безопасна: если у пользователя `diff.renames=false` или сработает `diff.renameLimit`, git сам покажет D+A — scope-проверка всё равно видит оба конца.
- Противоречия с ADR-0010 нет: D2 («footprint — это scope, гейт уже его применяет») этим изменением только усиливается, новой строки транскрипта не появилось.
- Каждая из четырёх сценариев spec.md покрыта тестом, который её называет в docstring; три новых теста действительно падают при откате на `--name-only` (первый — по `assert not passed`, второй — по списку `checked_paths`, третий — по появлению строки «journal-only transaction»).

Честно о пределах проверки: `uv run pytest` и `openspec validate --all` в этой песочнице запустить не дали (команды требуют approval), так что критерии «тесты проходят» и «validate проходит» я подтвердить исполнением не могу — только чтением. Структура архива совпадает с уже лежащим `2026-09-06-keep-review-prose`, а в `openspec/specs/scope-enforcement/spec.md` есть написанный Purpose и требования без дельта-заголовков.

Два неблокирующих замечания.

Docstring `scope_warnings` в `src/agentmarshal/journal/open_task.py:90` до сих пор утверждает, что гейт сравнивает записи scope с путями из `git diff --name-only` — после этого изменения перечисление другое; сам по себе аргумент («git перечисляет файлы, не каталоги») остаётся верным, а файл вне scope контракта, так что правка — в следующий кандидат.

Тест `tests/test_gate.py:369` проверяет точную последовательность вызовов `scope_covers` (`checked_paths == [...]`), а не факт, что источник переименования был проверен; это привязывает тест к порядку вызовов внутри `run_gate` и к тому, что контракт из `_gate_repo` не содержит `documents`/`extensions` — добавление любого из них сломает тест по не относящейся к сценарию причине. `assert "src/original.py" in checked_paths` дало бы то же падение при откате без этой связки.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "079a9cda0b6e931bb9c98fde2e841e1087abc9fc", "verdict": "approved", "findings": [], "advisory_findings": ["stale-name-only-reference-in-open-task-docstring", "brittle-exact-call-sequence-assertion"]}
AGENTMARSHAL_VERDICT_END
