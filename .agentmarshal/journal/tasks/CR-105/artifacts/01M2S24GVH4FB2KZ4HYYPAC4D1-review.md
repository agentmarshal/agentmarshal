Ревью закончено. Тесты я не запускал: запуск pytest через `uv` требовал разрешения, которого в этой сессии не было. Поэтому проверка статическая: чтение кода и сверка с контрактом.

Что проверено:
- **Главный сценарий.** Кандидат переоткрывает задачу, которая завершена в base, и больше ничего не меняет. Тест реально кладёт reopening в diff кандидата, а не коммитит его в base. Base-state check теперь такой кандидат пропускает.
- **Одно правило вместо двух.** Что допускается после закрытия задачи, gate спрашивает у `status.py` через `record_type_is_admitted_after_terminal`. Список суффиксов `-session.json` из `gate.py` убран.
- **Проекция не изменилась.** Поведение `project_status` и `load_task_for_record` осталось прежним, включая тексты ошибок.
- **Байтовые transcript-тесты.** Diff их не меняет, а старая строка PASS для measurements сохранена дословно.
- **ADR-0005.** Решение 3 в ADR запрещает lifecycle-записи после закрытия задачи, а reopening как раз такая запись. Это расхождение уже признано в статусной заметке самого ADR и в спеке `record-lifecycle`, так что блокером я его не считаю.
- **Сценарии спеки.** На каждый из четырёх сценариев delta spec есть тест, и docstring называет его.

Два замечания, оба не блокирующие:

A1 (advisory), `src/agentmarshal/journal/gate.py:672-679`. Правило «переоткрытая задача открыта» по-прежнему захардкожено в gate.py. Он всё ещё хранит свой кортеж суффиксов lifecycle-записей (`-completed.json`, `-abandoned.json`, `-reopened.json`) и решает, закрыта ли задача, через `endswith("-reopened.json")`. Та же информация есть в таблице проекции, которую открывает новый `projected_state_of`. Критерий приёмки это не нарушает: там запрещён только список *допускаемых* суффиксов. Но это ровно тот вид дублирования, который CR-102 назвал будущим расхождением.

A2 (advisory), `tests/test_gate.py:1629-1654`. Ни один тест не проверяет, что gate передаёт в правило именно terminal state из base. Кандидата с reopening абандоненной задачи и кандидата с review ещё раньше отклоняет проекция самого кандидата, так что ветка base-state check до них не доходит. Само правило покрыто отдельным unit-тестом, но если бы gate всегда передавал `"done"`, ни один тест бы не упал.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "4e0f6aafc518a5f6bb9f25c190680c2d339f4bdf", "verdict": "approved", "findings": [], "advisory_findings": ["A1-gate-keeps-lifecycle-suffix-encoding", "A2-gate-terminal-state-wiring-untested"]}
AGENTMARSHAL_VERDICT_END
