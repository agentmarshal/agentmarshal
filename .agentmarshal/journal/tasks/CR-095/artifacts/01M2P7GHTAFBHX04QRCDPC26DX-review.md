Проверил запись против кода, журнала и трёх названных решений. Фактические утверждения держатся: `gate.py:746` читает контракт из merge-base, `review.py:358-366` — из снапшота ревьюируемого коммита в embedded и из рабочего дерева сайдкара в сайдкаре, `gate.py:887` привязывает одобрение к `reviewed_commit`, `records.py:225-228` отказывает записи с полем, которого схема не допускает, `amend` (`cli.py:864-882`) пишет только запись и не трогает `contract.md`, а `submit-review` действительно существует как человеческий путь без промпта. Счёт 21/18/92 сходится с журналом: 21 запись amendment в 18 завершённых задачах, 92 задачи с записью `completed`. Ссылка на proposal 022 разрешается, `docs/adr/README.md` отсутствует — поправка к контракту описывает репозиторий верно. Блокирующего не нашёл; три замечания на точность.

**embedded-snapshot-already-carries-records** — `docs/adr/ADR-0011-contract-amendment-visibility.md:41-44` утверждает, что сломано «in both placements» одинаково, но размещения здесь расходятся, а критерий 2 требует отвечать про сайдкар отдельно именно там, где ответ другой. Ревьюер запускается с cwd в снапшоте (`src/agentmarshal/journal/review.py:195`), а снапшот — это `git archive` ревьюируемого коммита целиком (`review.py:302-322`), включая `.agentmarshal/journal/tasks/<id>/records/`. В embedded записи amendment физически достижимы ревьюеру, просто на них никто не указывает; я в этом ревью прочитал `CR-095/records/01M2N9GZ7DNJVKEKY77ARR8QMS-amendment.json` прямо из выданного мне дерева. В сайдкаре хост по ADR-0008 D3 не содержит журнала вовсе, и записи недостижимы по-настоящему. Дефект стоило бы сформулировать как «не сообщено» в embedded и «отсутствует» в сайдкаре.

**consequences-overstates-field-presence** — `docs/adr/ADR-0011-contract-amendment-visibility.md:140` говорит «its review records still carry the field below» без оговорки, тогда как Decision 4 (строки 113-116) явно делает поле необязательным и называет случай, когда его нет: `submit-review` пишет вердикт без промпта, хешировать нечего. Для задачи без поправок, отревьюенной человеческим путём, запись поля не несёт, так что утверждение в Consequences шире механизма, который оно описывает.

**unqualified-21-amendment-count** — `docs/adr/ADR-0011-contract-amendment-visibility.md:158` говорит «this project's 21 amendments» без квалификатора, который есть в строке 30 («across 18 of the 92 tasks completed»). На ревьюируемом коммите в журнале 22 записи amendment в 19 задачах: 22-я — `CR-095/records/01M2N9GZ7DNJVKEKY77ARR8QMS-amendment.json`, поправка к контракту самой этой задачи, помеченная в контракте разделом «Amended 2026-09-16». Число 21 верно только для завершённых задач, и в строке 158 это не сказано.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "3e12aae09a5bcdf4024cef4a0a2e0373216fcfd7", "verdict": "approved", "findings": [], "advisory_findings": ["embedded-snapshot-already-carries-records", "consequences-overstates-field-presence", "unqualified-21-amendment-count"]}
AGENTMARSHAL_VERDICT_END
