Проверил запись против контракта и против самого механизма в коде: `amend` (`src/agentmarshal/cli.py:864`), gate (`src/agentmarshal/journal/gate.py:334`, `:653`, `:735`, `:797`), review launcher (`src/agentmarshal/journal/review.py:347`), схема записей (`src/agentmarshal/journal/records.py:204`), ADR-0004 D3, ADR-0006, ADR-0008. Цифры журнала сходятся: 21 amendment-запись в 18 из 92 завершённых задач (текущая CR-095 со своей поправкой не в счёт), и CR-069 действительно имеет поправку между вторым и третьим раундом с раундами 3 и 4 после неё — критерий 5 выполнен, критерий 3 («Left open» плюс причины отказа у каждой альтернативы) тоже.

**F1-empty-rendering-reads-as-unchanged** (blocking) — в «Consequences» (`docs/adr/ADR-0011-contract-amendment-visibility.md:137-140`) сказано без оговорок, что reviewer и implementer узнают, что контракт менялся, «when and why», а задача без поправок «has nothing to render». Механизм рендерит только amendment-записи, а запись и правка документа ничем не связаны: `amend` пишет запись и не трогает `contract.md` (`src/agentmarshal/cli.py:864-882`), `contract.md` не входит в append-only-класс (`gate.py:334-339`), и любое изменение внутри `.agentmarshal/` проходит journal-only lane без review (`gate.py:653`, `gate.py:730-732`). Правка критерия мимо `amend` не даёт ни одной записи, пустой рендер читается как «текст такой же, каким был при открытии», и запись об этой границе молчит — это абсолютное утверждение шире механизма (критерии 1 и 4), причём ровно в той точке, ради которой ADR и написан. Сама же Decision 2 отвергает секцию в документе аргументом «непроверяемая копия хуже отсутствия, потому что выглядит авторитетно» — пустой рендер выглядит авторитетно по той же причине.

**A1-consequences-contradicts-optional-field** (advisory) — строка 140 «its review records still carry the field below» противоречит Decision 4 (`:113-116`), где `reviewed_contract` опционален и на human-пути `submit-review` отсутствует (подтверждается `src/agentmarshal/journal/submit_review.py`: контракт там не читается вовсе); вдобавок «below» указывает вперёд на поле, определённое выше.

**A2-sidecar-contract-not-pinned-anywhere** (advisory) — Decision 3 (`:92-96`) говорит «the contract is read from the sidecar's own working tree; it is versioned in the sidecar's history (ADR-0008), but nothing ties the text ... to the host commit». Gate сравнивает именно рабочую копию, которая может быть не закоммичена ни в одной истории — его собственная формулировка «contract read from the sidecar working tree, not pinned to a commit» (`gate.py:797-819`). Провенанс слабее, чем описан, а критерий 2 требует отвечать про sidecar отдельно там, где ответ отличается.

**A3-most-common-case-unsupported** (advisory) — «That matters most in the case that occurs most: the text changed because the previous round objected to it» (`:44-45`) не подкреплено ни одним из приведённых измерений: классификация из proposal 022 (1 repair, 2 contradiction, 3 additions) причинной связи с раундом review не измеряет, а по собственному журналу приведён один случай. Порядок записей такую версию допускает, но запись подаёт её как установленный факт.

**A4-nothing-could-check-it** (advisory) — причина отказа первой альтернативе (`:152-155`) «nothing could check it without parsing prose» шире механизма: секцию в фиксированных разделителях можно перегенерировать из записей и сравнить байтами, не парся прозу. Настоящая цена — вторая копия, которую надо держать в синхроне, — уже названа в Decision 2 и выдерживает проверку лучше, чем абсолют.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "3e12aae09a5bcdf4024cef4a0a2e0373216fcfd7", "verdict": "changes_required", "findings": ["F1-empty-rendering-reads-as-unchanged"], "advisory_findings": ["A1-consequences-contradicts-optional-field", "A2-sidecar-contract-not-pinned-anywhere", "A3-most-common-case-unsupported", "A4-nothing-could-check-it"]}
AGENTMARSHAL_VERDICT_END
