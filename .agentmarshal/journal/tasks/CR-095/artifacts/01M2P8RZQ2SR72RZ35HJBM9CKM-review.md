Проверил ADR против контракта и против кода/журнала.

**Что сверил фактически:**
- `21 amendment records across 18 of the 92 tasks completed` — сходится точно: в журнале 22 amendment-записи в 19 задачах, 92 `*-completed.json`; CR-095 (собственная поправка этой задачи) не завершена, значит 21/18/92 — верно. Контракт в Context говорит «91», ADR точнее.
- Случай «поправка между вторым и третьим раундом» — это CR-069: amendment после review #2, затем reviews #3 и #4, и причина поправки дословно про формулировку критерия после blocking-находки. ✓
- Цифры адоптера (6/4, 5 из 6, 9 ревью, 0 из 6) совпадают с proposal 022. ✓
- `gate.py:735-746` — контракт читается из merge-base tree; `gate.py:750` — в sidecar из рабочего дерева журнала. `review.py:360-368` — промпт ревьюера берёт контракт из снапшота reviewed commit (embedded) / из sidecar-журнала. `gate.py:884-887` — ревью привязано к `resolved_commit`. Всё, что ADR утверждает о текущем механизме, верно.
- `records.py:219-227` — валидация закрытая (лишнее поле отвергается), `needs_schema_4` — ровно тот паттерн «поле приходит через версию», на который ссылается Decision 4; ADR-0004 про floor-схему процитирован корректно. Дайджесты — `[0-9a-f]{64}` / `[0-9a-f]{40}`, lowercase hex. ✓
- Findings-lane ревью идут только человеческим путём (`cli.py:574-580`), так что «единственный случай опциональности — human path» не противоречит findings-полосе.
- Ссылки `../proposals/022-...`, ADR-0004/0006/0008 существуют; scope не превышен (изменён только сам ADR).

Критерии 1–5 выполнены: дефект описан ровно как он есть, обе сущности (рендер истории и `reviewed_contract`) отвечают «с какой стороны читается / когда / что при отсутствии / что при изменении», sidecar разведён отдельно там, где ответ другой, «Left open» есть, каждая отвергнутая альтернатива несёт причину, а не предпочтение.

Ниже — два необязательных замечания.

Строки 24, 45 и 174 не переносились после правки: они идут на 110–123 символа при том, что весь остальной файл аккуратно завёрнут в ~79 — это артефакт редактирования, видимый в diff как единственные «широкие» строки документа.

В Context (строки 40–43) фраза «A sidecar has no such protection» сформулирована шире, чем то, что Decision 3 говорит точно: в sidecar привязка ревью к SHA сохраняется — `gate.py:884-887` выполняется в обеих раскладках и не входит в embedded-ветку, — отсутствует только связь текста контракта с коммитом. Есть щадящее чтение («such protection» = всё свойство предложения целиком), но документ, у которого отдельный критерий требует защитимости каждого абсолютного утверждения, лучше не оставлять на выбор читателя.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "7c699ae342e8c3f4a4a470e9677344345b3f358f", "verdict": "approved", "findings": [], "advisory_findings": ["adv-unwrapped-lines-24-45-174", "adv-sidecar-no-such-protection-overbroad"]}
AGENTMARSHAL_VERDICT_END
