Проверил документ против контракта, против трёх названных решений и против самого репозитория. Факты в записи сходятся: 21 amendment-record в 18 из 92 завершённых задач — ровно то, что лежит в журнале (22-й record принадлежит самой CR-095 и в счёт завершённых не входит); CR-069 действительно получил amendment между вторым и третьим раундом ревью, причём после verdict `changes_required` с блокирующим finding о формулировке критерия. Код подтверждает и остальное: gate читает контракт из merge-base (`gate.py:735-748`), launcher — из снапшота ревьюируемого коммита, а в sidecar — из рабочего дерева sidecar (`review.py:360-369`); ревью привязано к точному SHA (`gate.py:886-889`); валидация записей закрытая, лишнее поле отвергается; `reason` у amendment обязателен, дайджесты в нижнем hex; контракт целиком выдаётся ровно двумя путями — review-prompt и `brief`. Цитата ADR-0004 D3 («Gates never parse prose») точна, и правило «штампуем новую схему только на записи, которая несёт поле» действительно записано в ADR-0004. Блокирующих расхождений с критериями приёмки не нашёл; три замечания — необязательные.

CR095-A1: в Decision 4 (строки 133-139) правило «писатель ставит новую схему только на запись, несущую поле» применено к полю, которое launcher, по тексту той же секции, пишет всегда («The launcher always has one»), то есть новую схему получит практически каждая review-record — а Consequences (строки 207-209) оценивают цену как «one optional field ... no line in any transcript» и ничего не говорят о том, что читатель предыдущего релиза перестанет читать новые review-записи. Это ровно то условие, которое ADR-0009 в своих Consequences (строки 242-250) назвал причиной, по которой прецедент «работает в другую сторону»: там аргумент держался на том, что большинство записей новых полей не несут.

CR095-A2: на строках 66-68 необязательность имени актора приписана ADR-0006 («which ADR-0006 leaves optional»), но ADR-0006 (строки 47-56) говорит обратное по смыслу — значение всегда выводится из окружения, при отсутствии совпадения записывается сама git-identity, «so a project that configures nothing still gets a value», а точные имена полей и схему решение явно отдаёт реализующей задаче. Необязательность — факт реализации (`records.py:139-141`, CHANGELOG: «omitted rather than guessed»), а не то, что решает ADR-0006.

CR095-A3: на строках 141-143 сказано, что случай, ради которого поле необязательно, — человеческий путь `submit-review`. Это не единственный писатель review-записей без prompt: `migrate.py:285` и `migrate.py:387` тоже создают их через `create_review_record` при миграции v1→v2, и контракта, который можно честно захешировать, там тоже нет.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "2fe84cdfdaa34705ba61477ce69d11ad40a06ec1", "verdict": "approved", "findings": [], "advisory_findings": ["CR095-A1", "CR095-A2", "CR095-A3"]}
AGENTMARSHAL_VERDICT_END
