Проверил диф против дерева снапшота: читал `gate.py` целиком вокруг review-bound блока, `cli.py:739-835`, новые тесты и их хелперы, шаблон, документ, оба ADR и оба openspec-документа. Тесты запустить не смог — среда не даёт выполнять `uv run` без подтверждения, поэтому выводы о тестах статические.

Что держится. Режим действительно заперт в одну ветку: новый `elif latest is None and not review_required` стоит внутри `else:` не-journal-only полосы (`gate.py:941-960`), после веток acceptance, так что кандидат с записью о ревью — approving или нет — попадает в прежний `check(approved, ...)` и не может быть спасён флагом. Путь мерджа чист: `complete.py:59` зовёт `run_gate` без `review_required`, то есть `True`, и флага у `complete` нет вовсе. Дефолтный транскрипт не двигается — обе новые строки и новая закрывающая строка CLI под флагом `report.review_not_examined`, который по умолчанию `False`. Все пять сценариев спеки покрыты тестами с docstring'ами, включая делегирование к пиннутому 0.3.0-тесту; делегирование безопасно, потому что пиннутый тест скипается через `pytest.skip()` внутри тела, а не маркером, и такой же приём уже есть в файле (`tests/test_gate.py:459`). Шаблон просит режим, `continue-on-error` снят, `tasks.md` отмечен.

`design-guard-never-existed` — `openspec/changes/archive/2026-09-17-gate-without-a-review/design.md:27-29` пишет «The template asks for the mode; it does not decide anything itself. **Its guard is gone**», но в шаблоне на базе коммита никакого стража не было: диф снимает только `continue-on-error: true`, а `if: github.event_name == 'pull_request'` остался на месте и стражем судимости не является. Страж из предыдущей задачи был отклонён в ревью и в `templates/github/agentmarshal-governance.yml` не приземлялся — named contract document описывает удаление, которого не происходило.

`doc-closing-line-overgeneralized` — `docs/github-enforcement.md:78-82` утверждает, что CI-прогон «closes with `gate: passed what it examined` rather than `gate: passed`», но предыдущий буллет того же списка (строки 75-77) как раз называет journal-only полосы (openings и completions), а на них review-блок не выполняется вообще (`gate.py:742-744`), `review_not_examined` остаётся `False`, и прогон закрывается ровно строкой `gate: passed`. Для половины pull request'ов, которые этот же абзац перечисляет, утверждение документа ложно.

`own-workflow-out-of-sync` — `.github/workflows/agentmarshal-governance.yml:3-4,33-39`: собственный governance-workflow проекта по-прежнему несёт `continue-on-error: true`, зовёт гейт без `--without-review` и в шапке заявляет «`gate` is advisory until review materialisation (Phase C). See docs/github-enforcement.md» — документ, который после этого изменения такой договорённости уже не описывает. Файл вне scope контракта, поэтому это замечание, а не требование, но в репозитории теперь лежит образец, противоречащий собственной документации.

`sidecar-closing-line-untested` — `src/agentmarshal/cli.py:823-827` вводит новую пользовательскую строку «gate: advisory checks passed what they examined; the review was not examined; decides no merge», и ни один тест её не закрепляет: новые CLI-тесты проверяют только embedded-путь, а `docs/sidecar.md:230` пиннит прежнюю формулировку. Формулировку sidecar + режим можно сломать, не уронив ни один тест.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "a9e1e1746c4ce986d6fd2c78836ad1cf7a3e6563", "verdict": "approved", "findings": [], "advisory_findings": ["design-guard-never-existed", "doc-closing-line-overgeneralized", "own-workflow-out-of-sync", "sidecar-closing-line-untested"]}
AGENTMARSHAL_VERDICT_END
