Проверил diff статически (запуск pytest в этой песочнице требует подтверждения, поэтому тесты я не гонял — сужу по коду). Механизм режима сам по себе сделан аккуратно: новая ветка `elif latest is None and not review_required` стоит после двух веток, каждая из которых требует `latest is not None`, так что порядок ничего не ломает; проверка независимости рецензента по-прежнему висит на `if latest is not None:` и потому не затронута; `review_required` по умолчанию `True`, так что закреплённый byte-for-byte транскрипт 0.3.0 не двигается. Обход через режим я не нашёл: кандидат с отказным ревью так же отказывается, а любое другое правило (scope, append-only, lifecycle) режим не трогает.

Ниже то, что не сходится с контрактом.

**F-001** — `docs/github-enforcement.md:36-40` и повторно `:70-73`, а также комментарий шаблона `templates/github/agentmarshal-governance.yml:51-53`, утверждают, что требование approved-independent-review всё-таки обеспечивается: «The merge authority enforces it by running the gate again, without the flag, at the moment the review exists». В описываемой этим же документом схеме Variant 2 такого второго прогона нет и быть некому: собственная таблица документа (`docs/github-enforcement.md:84-89`) говорит «Merge trigger: provider merge after required checks», «Gate role: authority, as a required check», и по всему репозиторию «merge authority» — это и есть сам gate (`docs/overview.md:67`, `docs/quickstart.md:344`). Adopter, который выполнит указание «Mark it REQUIRED», получит мердж, где ревью-требование не проверяет никто, — а документ говорит ему, что проверяет. Прежний текст был честен («advisory until review materialisation is in place»). Критерий приёмки 5 требует, чтобы документ описывал то, что теперь существует; он описывает несуществующий компенсирующий контроль. Шаблон несёт то же утверждение при отсутствии в нём такого job или шага.

**F-002** — сценарий «the pinned transcript still matches» из `openspec/specs/gate-lanes/spec.md:50-54` (и из `openspec/changes/archive/2026-09-17-gate-without-a-review/specs/gate-lanes/spec.md`) не имеет теста, чей docstring его называет. Критерий 1 выводит из этого требования только два пункта раздела 2 tasks.md (шаблон и документ), а этот сценарий — не они. В репозитории уже есть ровно тот приём, которым это решается: `tests/test_gate.py:400-415`, `test_a_candidate_without_renames_prints_the_transcript_it_printed_before`, существует только чтобы назвать сценарий и делегировать в закреплённый тест, «it is pinned and must stay as it is, so this test names the scenario and delegates rather than copying it». Для нового сценария этот приём не применён.

**F-003** — `openspec/changes/archive/2026-09-17-gate-without-a-review/design.md:33-35` записывает митигацию риска «An operator reads "not examined" as "passed"» как уже действующую: «the summary counts it apart, as `doctor` now does for a precondition». У `doctor` это правда — `src/agentmarshal/cli.py:406-417` печатает «Summary: … N precondition(s) left to the operator». У gate ничего подобного нет: `run_gate` возвращает только строки, а `_run_gate` заканчивает одним `gate: passed` (`src/agentmarshal/cli.py:819-822`), побайтово тем же, что и у прогона, осмотревшего всё. Ни счёта, ни отличия в итоговой строке. Критерий 1 требует либо следовать design.md, либо записать в design.md, почему отступили; не сделано ни того, ни другого, и незакрытым остаётся именно тот риск, который threat model контракта формулирует как «must be able to tell the two apart at a glance».

**A-004** — флаг `--without-review` нигде не покрыт тестом: все четыре новых теста зовут `run_gate(..., review_required=False)` напрямую (`tests/test_gate.py:232-235`), а проводка `review_required=not args.without_review` (`src/agentmarshal/cli.py:795`) не проверяется ничем. Инверсия или опечатка здесь вернула бы ровно тот отказ на каждом implementation PR, который задача и чинит, при полностью зелёном наборе тестов, — притом что шаблон опирается именно на CLI-вызов. В файле есть готовая практика прогона через `main(["gate", ...])` (например `tests/test_gate.py:345-358`).

**A-005** — `gate --findings --without-review` молча принимает флаг, который на этой полосе ничего не значит: `_run_gate` уходит в ветку findings и возвращается до места, где `review_required` вообще вычисляется (`src/agentmarshal/cli.py:755-770`). Строкой выше в той же функции бессмысленная комбинация `--findings` с `--commit`/`--base` отвергается явно, так что молчание здесь выбивается из принятого в файле стиля.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "10a515f13004697066022184716dec6d04eaa31d", "verdict": "changes_required", "findings": ["F-001-doc-and-template-assert-a-second-gate-run-that-does-not-exist", "F-002-pinned-transcript-scenario-has-no-test-naming-it", "F-003-design-asserts-a-summary-count-the-gate-does-not-have"], "advisory_findings": ["A-004-cli-flag-wiring-is-untested", "A-005-without-review-silently-ignored-on-the-findings-lane"]}
AGENTMARSHAL_VERDICT_END
