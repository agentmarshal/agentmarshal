Разобрался с диффом, прочитал ADR-0002/0005, спеку `gate-lanes`, `design.md`, и проверил статически всю логику гейта (тесты запустить не дали — Bash тут требует апрув, так что проверка чисто по коду).

**Что сделано корректно.** Ветка `elif latest is None and not review_required` стоит после обеих acceptance-веток, каждая из которых требует `latest is not None`, так что режим действительно достижим только при отсутствии review-записи на коммит кандидата. Проверка независимости рецензента (`gate.py:969`) уже была обусловлена `latest is not None`, поэтому режим ничего в ней не меняет. `review_required=True` по умолчанию, новая ветка при дефолтном запуске недостижима, транскрипт байт в байт не двигается; `complete.py:59` и sidecar-путь `cli.py:866` флаг не передают, так что собственный merge-путь проекта режимом не пользуется — ровно как обещает `design.md`. Пустой `task` из ветки без `CR-<n>` доходит до `validate_task_id("")` и падает в `GateError` → exit 1, то есть утверждение в доке «Without one the gate job fails» верное.

Теперь замечания.

**F-001** — `docs/github-enforcement.md:93-103`, секция «Variant 1 vs Variant 2», не переписана и теперь противоречит новому тексту в том же файле. Строка 99 говорит, что на GitHub review evidence «materialised from the PR approval (Phase C)» — это ровно то, чего нет, и что строки 41-47 прямо называют непоставленным. Строка 100 называет роль гейта на GitHub «authority, as a required check», тогда как строка 43-44 в том же документе пишет «this project ships no merge authority for this provider». Строка 102 утверждает «The gate logic is identical across both; only the binding differs» — после этого изменения логика как раз не идентична: Variant 2 зовёт гейт с `--without-review`, что меняет набор оцениваемых проверок, а не только привязку к merge. Критерий приёмки 5 требует, чтобы этот файл описывал то, что теперь существует; хвост файла описывает то, чего нет, и это тот же класс переоценки enforcement'а, из-за которого была подана proposal 017 и отклонена предыдущая попытка.

**F-002** (advisory) — `templates/github/agentmarshal-governance.yml:47` открывает комментарий словами «Merge authority (Variant 2)», хотя через шесть строк тот же комментарий говорит, что approved-independent-review этой job'ой не enforce'ится и ничем на этом провайдере не enforce'ится. Оговорка рядом, поэтому не блокирующе, но заголовок стоит привести к тому же тону, что и остальной комментарий.

**F-003** (advisory) — `cli.py:826-831`: ветка sidecar проверяется раньше `report.review_not_examined`, поэтому sidecar-прогон с `--without-review` и без review-записи закрывается строкой `gate: advisory checks passed; decides no merge` и про неосмотренный review в закрывающей строке не говорит. `design.md` в разделе Risks обещает, что «a run that left the review unexamined says so on its closing line», без оговорки про placement; строки `NOT EXAMINED:` в транскрипте при этом есть, так что молчания нет, но обещание design.md выполняется не везде и departure в design.md не записан.

**F-004** (advisory) — `tests/test_gate.py:232-244`: `test_a_default_run_prints_the_transcript_it_printed_before` — чистый алиас, который вызывает `test_embedded_diff_lane_transcript_matches_published_030_byte_for_byte` напрямую. Тот скипается через `pytest.skip(SKIP_030)`, если бинарь 0.3.0 не найден (`tests/test_gate.py:414-416`), то есть в обычном окружении сценарий, который этот тест именует, не исполняется вовсе. Плюс прямой вызов тестовой функции из другой ломается молча, если у вызываемой появится фикстура или маркер. Инвариант закрывающей строки дефолтного прогона реально держит незапускаемый-по-условию `test_gate_passes_a_clean_candidate:405`, но сценарий спеки формально прикрыт скипающимся алиасом.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "6e16b9b750d3886d03005ecbf289c59299919885", "verdict": "changes_required", "findings": ["F-001"], "advisory_findings": ["F-002", "F-003", "F-004"]}
AGENTMARSHAL_VERDICT_END
