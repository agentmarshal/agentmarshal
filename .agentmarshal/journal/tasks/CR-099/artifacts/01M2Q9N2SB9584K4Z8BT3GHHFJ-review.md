Проверил снапшот на `ed2556aa01e7ebfeea2f211407fbdf45a720e292`: прочитал `run_gate`, CLI-обвязку, новые тесты, шаблон, документ, спеку и архив изменения, ADR-0002 и ADR-0005. Запустить тесты не смог — `uv run pytest` и `python -m pytest` требуют подтверждения в этой сессии, поэтому критерий «прогон без режима байт-в-байт тот же» проверен чтением кода, а не выполнением: единственная новая ветка в `gate.py:938` закрыта условием `not review_required`, дефолт — `True`, остальные правки аддитивны (новый флаг парсера и проброс), так что путь по умолчанию недостижимо изменён.

Существо контракта выполнено: режим меняет ровно случай «записи ревью нет» (`gate.py:938-953`), кандидат с ревью — одобряющим или нет — судится прежними ветками (`gate.py:955-983`), отказ по scope под режимом сохраняется, обе непроверенные проверки печатаются строками в той же форме, что и лейн findings (`gate.py:141-261`), все пять сценариев спеки закрыты тестами, чьи docstring их называют, чекбоксы в tasks.md отмечены, `continue-on-error` из job'а ушёл. Блокирующих дефектов не нашёл; ниже — четыре замечания без блокировки.

**docs-merge-authority-that-does-not-exist** (advisory) — `docs/github-enforcement.md:74-76`: второй пункт «Until it lands» утверждает, что «the merge authority runs the gate again without the flag», хотя раздел выше (`:37-43`) прямо говорит, что для этого провайдера merge authority не поставляется, а сам шаблон называет merge authority именно тот job, который запускается **с** флагом (`templates/github/agentmarshal-governance.yml:47`). Адаптер, читающий открытый пункт, заключит, что где-то на его стороне гейт перезапускается без флага; этого не происходит.

**required-gate-hard-fails-an-unnamed-branch** (advisory) — `docs/github-enforcement.md:30-35` и `templates/github/agentmarshal-governance.yml:74`: документ теперь велит пометить `gate` как REQUIRED, а `continue-on-error` снят, но task извлекается из имени ветки (`grep -oE 'CR-[0-9]+'`); при ветке без `CR-<n>` получается `--task ""`, `load_task_status` падает, гейт выходит с 1 — и merge блокируется наглухо, тогда как раньше это поглощалось терпимостью job'а. Требование к именованию веток записано только в `docs/self-hosting-workflow.md:119-120`, а не в том документе, на который ссылается шаблон про branch protection.

**own-workflow-contradicts-the-shipped-template** (advisory) — `.github/workflows/agentmarshal-governance.yml:3,39`: собственный workflow проекта остался с `continue-on-error: true` и комментарием «`gate` is advisory until review materialisation», то есть проект поставляет шаблон в схеме, которую его же CI опровергает. Файл вне scope контракта, чинить здесь нельзя — нужна отдельная задача.

**test-duplicates-require-changes-helper** (advisory) — `tests/test_gate.py:345`: новый тест вписывает полный вызов `submit-review` с `changes_required`, хотя рядом уже есть хелпер `_require_changes` (`tests/test_gate.py:176`), дающий ровно ту же запись; `_require_changes(repo, head, "F-001")` короче и не даёт двум копиям набора аргументов разойтись.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "ed2556aa01e7ebfeea2f211407fbdf45a720e292", "verdict": "approved", "findings": [], "advisory_findings": ["docs-merge-authority-that-does-not-exist", "required-gate-hard-fails-an-unnamed-branch", "own-workflow-contradicts-the-shipped-template", "test-duplicates-require-changes-helper"]}
AGENTMARSHAL_VERDICT_END
