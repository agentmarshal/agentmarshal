Разобрал контракт, диff и текущее состояние репозитория. Предикат `forges_rendered_text` реализован ровно как описано (Cc, Cs, Zl, Zp + двенадцать bidi-кодпоинтов), обе обёртки и inline-проверка в `validate.py` теперь зовут его, `str.isprintable()` из кода ушёл, цикла импортов `contracts → records` нет, сообщения об отказе сохранены, все семь сценариев дельта-спеки покрыты тестами с называющими их docstring'ами. Тесты запустить в этой среде не дали (sandbox отклонил `pytest`), поэтому проход CI я проверить не мог — выводы ниже статические.

**CR-114-F001** — `openspec/specs/record-text-safety/spec.md:14` (и идентично в архивной дельте `openspec/changes/archive/2026-09-24-narrow-the-forgeable-text-rule/specs/record-text-safety/spec.md:12`, и в `design.md:5`) перечисляет среди мест, где правило применяется, «a contract header's `scope`, `documents`, `decisions` and `extensions` entries». Для `scope` это неверно: в `parse_contract_text` (`src/agentmarshal/journal/contracts.py:157-180`) `decisions` идут через `reject_control_characters`, `documents` — через `validate_scope_entry`, `extensions` — через `reject_control_characters`, а `scope` берётся голым `_require_string_array(data, "scope")` без поэлементной проверки. Во всём `src/` нет ни одного вызова `validate_scope_entry`/`reject_control_characters` для записей `scope`. Это не абстрактная неточность: `open_task.py:65` пишет элементы scope через `json.dumps`, то есть `\n` в контракте сериализуется как валидный TOML-escape, а `gate.py:113-115` и `gate.py:812-817` склеивают `contract.scope` в одну строку вывода — ровно та поверхность подделки строки, которую требование объявляет закрытой. Новая нормативная capability, названная в контракте как документ (`documents = ["openspec/specs/record-text-safety/"]`), утверждает поведение, которого в коде нет, и ни один тест его не пиннит.

**CR-114-F002** (advisory) — то же перечисление в `openspec/specs/record-text-safety/spec.md:13-17` пропускает записи extension-манифеста: `src/agentmarshal/journal/extensions.py:65-70` прогоняет их через `validate_scope_entry`, то есть они подчиняются этому же набору символов. Здесь спека не врёт, а недоговаривает, но раз уж требование перечисляет места поимённо (включая `an extension's name`), пропуск footprint-записей делает список неполным и провоцирует ту самую drift, ради устранения которой задача и заводилась.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "1a320f7639278d721986f7ac28db7352b3c0cbdb", "verdict": "changes_required", "findings": ["CR-114-F001"], "advisory_findings": ["CR-114-F002"]}
AGENTMARSHAL_VERDICT_END
