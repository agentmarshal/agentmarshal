Вердикт: **approved**, блокирующих замечаний нет. Одна оговорка: запустить здесь ничего не удалось. Вызовы `python3 -m agentmarshal --version` и `pytest` упёрлись в запрос разрешения, `uv` в окружении нет. Поэтому `--version` и прохождение тестов, включая новый `tests/test_quickstart.py`, я проверил только чтением кода, а не запуском.

**Критерии контракта:**

1. **CHANGELOG 0.4.0 от 2026-09-18.** Выполнен. Все 25 задач с CR-084 по CR-108 названы, у каждой есть запись о завершении в журнале, и темы разделов совпадают с заголовками их контрактов. Выборочно сверил с кодом:
   - схемы 4, 5 и 6 (`records.py:144-153`, `:957`, `:879`);
   - опции CLI `--reviewed-finding`, `--findings`, `--without-review`, `--dry-run`, `--prose`;
   - лимит в 20 хитов leak-scan (`gate.py:68`, `capture.py:324`);
   - строки `NOT EXAMINED` для проверок, завязанных на ревью (`gate.py:949-954`);
   - отказ при дрейфе и при отсутствии проверяемых артефактов (`review.py:739-770`).

   Ограничения по GitHub оговорены, ссылки ведут на опубликованные документы, слов «planned», «next» и «soon» нет.
2. **UPGRADING.** Выполнен, все обязательные пункты на месте:
   - схемы 4, 5 и 6. Верно сказано, что `agentmarshal review` всегда пишет `reviewed_contract` (`review.py:883`), а `submit-review` этого поля не пишет;
   - отказ writer'а (CR-102);
   - reopen проходит gate (CR-105);
   - `--without-review` и изменение шаблона;
   - новый формат вывода leak-scan.

   Дополнительно описаны перенос `artifacts/`, где `validate` действительно проверяет pinned-артефакты (`validate.py:56`), и rename в gate.
3. **Версия.** Выполнен. `0.4.0` стоит в `pyproject.toml`, `__init__.py` и `uv.lock`, `test_smoke` обновлён.
4. **Упоминания релиза.** Выполнен. Фразы о текущем релизе переведены на 0.4.0. Исторические упоминания («New in 0.3.0», «Sidecar is new in 0.3.0», «This is a 0.3.0 capability») оставлены как есть. Тест, который побайтно сверяет транскрипт с опубликованным 0.3.0, в diff не менялся.
5. **Три advisory от CR-107.** Выполнен:
   - в `docs/overview.md` раздел roadmap больше не называет направления «designed», и у каждого пункта сказано, есть ли опубликованный дизайн;
   - в proposal 005 фраза про «next in line» помечена как устаревшая, а ниже добавлен датированный раздел о том, что произошло на самом деле;
   - в job `governance` вернули комментарий из шаблона, и он побайтно совпадает с `templates/github/agentmarshal-governance.yml:24-27`.

**Что не удалось проверить:**
- Фразу в quickstart о том, что все команды прогонялись на wheel 0.4.0 до публикации: в снапшоте нет следов такого прогона.
- Совпадение вывода gate с транскриптом в `test_quickstart.py` на деле. По чтению кода строки gate (`gate.py:720`, `:103`) и формат транскрипта в шаге 5 совпадают.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "eb6f57fd6cea2677a738807122f045b331a04d9e", "verdict": "approved", "findings": []}
AGENTMARSHAL_VERDICT_END

Три коннектора claude.ai (Gmail, Google Calendar, Google Drive) ждут авторизации в настройках коннекторов claude.ai. Для этого ревью они не понадобились.
