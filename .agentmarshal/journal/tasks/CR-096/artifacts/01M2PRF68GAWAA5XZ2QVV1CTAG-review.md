I reviewed the diff against the contract, the two named decisions, and the OpenSpec material. Findings below.

**F-001** — `openspec/specs/review-evidence/spec.md:108-111` carries a normative clause — "and that schema SHALL be accepted only for review records, since no other record type is handed a contract" — that appears in no change delta: the archived delta at `openspec/changes/archive/2026-09-17-render-amendment-history/specs/review-evidence/spec.md:3-6` stops at "SHALL declare a schema that allows it". The sibling capability in this same commit proves the convention (`diff` of the `contract-history` delta against its published spec differs only by the `# … Specification` / `## Purpose` / `## Requirements` framing, the requirement body being identical), so the published `review-evidence` spec is not the delta applied — a requirement was hand-written straight into the published spec. The unwrapped line 110, which overruns the 80-column prose width every other line in the file respects, is the tell.

**F-002** — `src/agentmarshal/journal/records.py:213-214` enforces a rule that `design.md` never decides and never records a departure for, which acceptance criterion 1 requires ("the implementation follows design.md's decisions or records in design.md why it departed"). `design.md`'s only schema-5 decision is "Schema 5 follows the shape of schema 4": `_SCHEMA_5_FIELDS`, `_SUPPORTED_SCHEMAS` gains 5, the writer stamps 5 only when the field is present, the message names the field. Schema 4 imposes no per-record-type ceiling — `records.py:289-293` is a *floor* (`needs_schema_4 … and schema < 4`), not a restriction on which types may declare it — so a type ceiling is an added refusal, not the same shape, and `design.md` has only Context/Decisions/Risks with nothing recording it. Separately the guard is written `schema >= 5`, not `schema == 5`: the schema number is global across record types here (`_SUPPORTED_SCHEMAS` is one set, `create_session_record` and friends all stamp from the same sequence), so the first future schema 6 added for any non-review record type — a session or acceptance field — is refused at this line with "record schema 5 is only supported for review records", a message that will not even name the schema at fault.

**A-001** (advisory) — `src/agentmarshal/journal/brief.py:325` and `src/agentmarshal/journal/review.py:398-400` call `read_records()` for a second full read-and-validate pass over every record file of the task, although `load_task_status()` — already called at `brief.py:280` and `review.py:361` — returns exactly those validated records in the same order as `task.records` (`status.py:85,96`). Both sites can pass `list(task.records)`. As written every `brief` and every `review` re-opens, re-parses and re-validates every record file of the task; in `launch_review` the second read lands after snapshot extraction, so the prompt's history and the status projection the same command already made can be built from two different reads of the directory.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "293e6abe9f3bf78d58d62599859e4971c65e215e", "verdict": "changes_required", "findings": ["F-001", "F-002"], "advisory_findings": ["A-001"]}
AGENTMARSHAL_VERDICT_END

Кратко по-русски: механизм сам по себе рабочий — рендеринг истории поправок и поле `reviewed_contract` сделаны аккуратно, сценарии покрыты тестами с называющими их docstring'ами, гейт не тронут. Блокируют две вещи: в опубликованный spec вписано требование, которого нет ни в одной дельте, и код в `records.py:213` реализует ограничение, которого нет в `design.md` (и записи об отступлении там тоже нет), причём через `>=`, что закроет все будущие схемы для не-review записей.
