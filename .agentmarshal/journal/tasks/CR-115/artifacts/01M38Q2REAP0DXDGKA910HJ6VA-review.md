Проверил diff против контракта, а также реальное состояние snapshot'а: версии (`pyproject.toml:3`, `src/agentmarshal/__init__.py:3`, `uv.lock:11`, `tests/test_smoke.py:48` + `test_console_version_prints_package_version`), все install-пины (`README.md:77`, `docs/quickstart.md:26`, `docs/sidecar.md:75`), `framework.version` в примере project.json (`docs/sidecar.md:125` совпадает с тем, что пишет `project.py:130` из `__version__`), точность трёх записей CHANGELOG против кода (`records.py:489-524` — категории `Cc/Cs/Zl/Zp` + bidi, включая embeddings; `release.yml:40-48` — отказ публиковать dev/local; `docs/quickstart.md:445-451` — `provider-limit` и «token counts are not what a provider charges»), заголовок `0.4.0 → 0.4.1` и фразу «nothing … requires action» в `UPGRADING.md:3-7`, неизменность секции 0.4.0 и истории. Транскрипты quickstart не содержат строк версии, так что пин и текст не расходятся. Остаётся один блокирующий дефект.

**contributing-versioning-rule-contradicts-this-release** — `CONTRIBUTING.md:122-131`: переписанное правило («the default branch **always** carries the version of the release being prepared») опровергается именно этим релизом: пока готовился 0.4.1, ветка несла `0.5.0.dev0` (см. `tool_version` во всех записях CR-112/CR-113/CR-114 и в `.agentmarshal/journal/tasks/CR-115/records/01M38N4QS068QJ67PC8FTDD90C-opened.json`), а не `0.4.1.dev0`; никакого механизма, который переписал бы версию ветки на `0.4.2.dev0`, «if another patch came first», в проекте нет — и сама фраза «The task that prepares a release removes the suffix» для этой задачи неверна: суффикс не снят, номер заменён (`0.5.0.dev0` → `0.4.1`). Вдобавок в том же предложении утверждается как факт, что «0.5.0 is the release being prepared», — непубликованный релиз, которого нет ни в одном опубликованном плане (журнал уже блокировал этот класс в CR-109, `proposals-index-promises-0.5.0`), и это утверждение противоречит хедж-оговорке в своей же строке. Правка требовалась amendment'ом именно потому, что старый пример «this release contradicts», — замена противоречит релизу так же, и вне scope остаётся комментарий `.github/workflows/release.yml:40` «carries the next version», который теперь расходится с новой формулировкой. Честное описание: задача релиза ставит версию того релиза, который готовит, а первая задача после тега ставит `.dev0` следующей ожидаемой версии.

**readme-status-omits-0-4-1** (advisory) — `README.md:56-70`: Status говорит «Version 0.4.1 ships…», но самая свежая пометка «New in» — 0.4.0, так что описание пакета, которое PyPI покажет для 0.4.1, нигде не упоминает сам фикс; адоптер, которому 0.4.0 отказал, узнаёт про него только пройдя по ссылке на CHANGELOG. Одна фраза «New in 0.4.1: `validate` reads a journal 0.4.0 refused (CR-114)» закрыла бы это, не трогая историю 0.4.0.

**out-of-scope-docs-still-present-0-4-0-as-current** (advisory) — вне scope, поэтому здесь не исправимо, но станет неверным в момент тега: `docs/overview.md:146,149` называют 0.4.0 «the shipped repository version» и перечисляют, чего «not in 0.4.0»; `docs/proposals/README.md:81` пишет, что outcome-значение из proposal 024 живёт на «default branch», хотя оно уходит в 0.4.1.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "8c2bbd3833bad565f359210e1a7a73285e9080b7", "verdict": "changes_required", "findings": ["contributing-versioning-rule-contradicts-this-release"], "advisory_findings": ["readme-status-omits-0-4-1", "out-of-scope-docs-still-present-0-4-0-as-current"]}
AGENTMARSHAL_VERDICT_END
