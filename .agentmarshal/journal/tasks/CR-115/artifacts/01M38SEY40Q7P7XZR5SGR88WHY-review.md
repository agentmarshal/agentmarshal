Проверил дифф по файлам снапшота: версии, тексты, код правила CR-114, release workflow и журнал задач.

Что сошлось:

- Версия 0.4.1 в `pyproject.toml:3`, `src/agentmarshal/__init__.py:3`, `uv.lock:11`, `tests/test_smoke.py:48`; `--version` печатает именно `__version__` (`src/agentmarshal/cli.py:86-88`), так что критерий про `agentmarshal --version` держится.
- CHANGELOG 0.4.1 датирован днём релиза, в нём ровно три задачи, приземлившиеся после тега (CR-112, CR-113, CR-114 — в журнале у всех трёх есть `completed`, CR-111 и раньше входят в секцию 0.4.0), и записи описывают смёрженное поведение: правило в `src/agentmarshal/journal/records.py:489-524` действительно перечисляет `Cc`, `Cs`, `Zl`, `Zp` и bidi-контролы и решается одним предикатом `forges_rendered_text`; отказ публиковать dev/local-версию реально стоит в `.github/workflows/release.yml`; утверждение про токены проверяется по `docs/quickstart.md:445-451` и отсутствию «cost» в README.
- Секция UPGRADING озаглавлена `0.4.0 → 0.4.1` и прямо говорит, что больше ничего действий не требует; секции про 0.3.0/0.2.0/0.1.0 как история не тронуты.
- Все install-команды, которые читатель может выполнить как «поставить текущий релиз», называют 0.4.1 (README, quickstart, sidecar), а пример `framework.version` в sidecar согласован с тем, что пишет `init` (`tests/test_smoke.py:77`). Ни одного оставшегося «текущий релиз = 0.4.0» в docs не нашёл; `0.5.0.dev0` остался только как прошедшее время в CONTRIBUTING.

Прогнать полный CI я здесь не могу (запуск pytest не разрешён в этой песочнице), так что критерий про CI и про read-only `validate` по журналу адаптера остаётся на отчёт в completion — в самом диффе ничего, что бы его ломало, нет: единственный тест, привязанный к версии, обновлён.

Две необязательные придирки ниже.

В `UPGRADING.md:9-11` абзац-редирект говорит «The section below it describes the step from 0.3.0», но непосредственно ниже идёт подсекция `### A review record refused over a space separator` того же раздела, а раздел `## 0.3.0 → 0.4.0` начинается только на строке 50 — читатель, идущий по тексту вниз, сначала упирается не в ту секцию. Там же сказано, что та секция «names `==0.4.0` throughout» (то есть про пины), но в ней есть и шаг 3 `UPGRADING.md:78` — «Verify `agentmarshal --version` reports `0.4.0` everywhere», который перенаправленный на 0.4.1 читатель выполнит буквально и получит несоответствие. Оговорка про пин это не покрывает.

В `docs/proposals/README.md:109` строка предложения 019 говорит «not in 0.4.0», то есть утверждение о том, чего нет в поставляемом релизе, сформулировано относительно предыдущего номера; после тега читатель на 0.4.1 читает его как про чужую версию. Файл в scope, хотя правка вне того, ради чего он был туда добавлен, и фраза формально остаётся истинной — поэтому не блокирующее.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "da1bb6b778f885455cd0692847ffec6093a0644c", "verdict": "approved", "findings": [], "advisory_findings": ["upgrading-redirect-points-at-wrong-section-and-leaves-version-verify-step-at-0-4-0", "proposals-index-019-row-states-absence-relative-to-0-4-0"]}
AGENTMARSHAL_VERDICT_END
