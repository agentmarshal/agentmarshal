Проверил дифф против контракта CR-115: версия 0.4.1 стоит в `pyproject.toml:3`, `src/agentmarshal/__init__.py:3`, `uv.lock` и `tests/test_smoke.py:48`, `--version` печатает `__version__` (`src/agentmarshal/cli.py:86-88`), секция CHANGELOG датирована днём релиза и покрывает CR-112/CR-113/CR-114, запись про фикс говорит, что делать установке, которой 0.4.0 отказал; UPGRADING переименован в «0.4.0 → 0.4.1» и говорит, что больше ничего не требует действий; описание правила в CHANGELOG/UPGRADING совпадает с кодом (`records.py:489-525`, один предикат используется в `contracts.py:64` и `validate.py:72`); install-пины в README, quickstart и sidecar названы 0.4.1, история 0.4.0 в CHANGELOG не тронута.

Одна блокирующая находка и две advisory.

**`contributing-names-an-unplanned-0-5-0`** — `CONTRIBUTING.md:123-127` вместо того, чтобы убрать пример, который этот релиз опроверг, утверждает как факт: «after 0.4.1 it reads `0.5.0.dev0`, **because 0.5.0 is the release being prepared**», и тут же в том же предложении допускает «it would read `0.4.2.dev0` if another patch came first instead». Ни один документ и ни одна запись журнала не планируют 0.5.0 (grep по `docs/`, `openspec/`, `CHANGELOG.md` даёт только это предложение и `tool_version` в старых записях) — ровно та же ситуация, что и с прежним «after 0.4.0 it reads 0.5.0.dev0», который сломался, как только вместо 0.5.0 вышел 0.4.1. Это нарушает критерий «no statement names a release, task or document that is not published», причём в файле, который поправка 2026-09-24 внесла в scope именно из-за этого утверждения. Плюс предложение само себе противоречит: «0.5.0 — готовящийся релиз» и «может оказаться 0.4.2» одновременно. Правило нужно сформулировать так, чтобы оно не называло конкретный следующий номер, либо назвать его без утверждения о плане.

**`quickstart-wheel-claim-unverified`** (advisory) — `docs/quickstart.md:6` теперь заявляет, что каждая команда раздела «The governed loop» прогонялась на wheel `agentmarshal` 0.4.1, собранном до публикации. Поправка контракта требует, чтобы это утверждение оставалось истинным для названного wheel, но в снапшоте нет подтверждения прогона: `tests/test_quickstart.py:18-27` исполняет блоки из дерева исходников, а не из wheel, а в `.agentmarshal/journal/tasks/CR-115/records/` пока только `opened` и `amendment`. Прогон на wheel 0.4.1 должен быть назван в completion рядом с CI и read-only `validate` по журналу адоптера.

**`shipped-version-statements-left-at-0-4-0`** (advisory) — README и quickstart теперь говорят о текущем релизе как о 0.4.1 («remain inactive in 0.4.1», «0.4.1 acts on `reviews` alone», «in 0.4.1 a match does not block»), а те же самые утверждения в `docs/overview.md:146-167` («the shipped 0.4.0 repository version», «not active in 0.4.0», «the `reviews` class is read in 0.4.0», «0.4.0 ships a standalone `leak-scan`»), в `docs/adr/ADR-0005:26`, `docs/adr/ADR-0004:105` и в диспозиции 024 (`docs/proposals/README.md:81`, `docs/proposals/024-...md:72` — «default branch … ship with the next release») остались на 0.4.0. Читатель приходит в overview прямо из quickstart и видит другой номер шипнутой версии. Эти файлы не входят в scope CR-115, поэтому находка не блокирующая — но её стоит закрыть отдельной задачей до или сразу после пуша тега.

AGENTMARSHAL_VERDICT_BEGIN
{"reviewed_commit": "8c2bbd3833bad565f359210e1a7a73285e9080b7", "verdict": "changes_required", "findings": ["contributing-names-an-unplanned-0-5-0"], "advisory_findings": ["quickstart-wheel-claim-unverified", "shipped-version-statements-left-at-0-4-0"]}
AGENTMARSHAL_VERDICT_END
