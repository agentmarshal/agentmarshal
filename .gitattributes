# Text files are normalized to LF in the repository and checked out as LF
# on every platform. Tooling must not assume LF-only input from the working
# tree of other repositories; this policy governs this repository only.
* text=auto eol=lf

# Binary files are never normalized.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary
*.gz binary
